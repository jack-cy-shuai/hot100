# 栈与队列 · 机试核心速记手册

> 今日覆盖：6 题 | 栈匹配 / 单调队列 / 双栈双队列 | 100% 必考题型

---

## 一、本板块核心思想

| 数据结构 | 核心性质 | 适用场景 |
|----------|----------|----------|
| **栈** | 后进先出（LIFO） | 匹配问题、嵌套问题、最近相关性 |
| **队列** | 先进先出（FIFO） | 顺序性问题、滑动窗口、层次遍历 |

> 铁律：**所有栈题必须先判空，再访问栈顶！**

---

## 二、6 题逐题复盘

### 232. 用栈实现队列 ⭐

[LeetCode 232](https://leetcode.cn/problems/implement-queue-using-stacks/)

- **核心**：双栈分工——`inStack` 只管 push，`outStack` 只管 pop/peek
- **易错**：必须等 `outStack` 完全空了才能从 `inStack` 倒元素，且一次性倒完；`empty()` 两个栈都要判

```cpp
class MyQueue {
    stack<int> inStack;   // 输入栈：只收 push
    stack<int> outStack;  // 输出栈：只出 pop/peek

    void transfer() {
        if (outStack.empty()) {               // 输出栈空了才倒
            while (!inStack.empty()) {
                outStack.push(inStack.top());
                inStack.pop();
            }
        }
    }
public:
    void push(int x) { inStack.push(x); }

    int pop() {
        transfer();
        int res = outStack.top(); outStack.pop();
        return res;
    }

    int peek() {
        transfer();
        return outStack.top();
    }

    bool empty() {
        return inStack.empty() && outStack.empty();  // 两个都空才是空
    }
};
```

> 口诀：**输入只管压，输出空了倒，倒完再取顶，两个都空才是空**

---

### 225. 用队列实现栈 ⭐

[LeetCode 225](https://leetcode.cn/problems/implement-stack-using-queues/)

- **核心**：单队列——每次 push 后把前面元素全部移到新元素后面
- **易错**：循环次数是 `q.size() - 1`，留新元素在队首；单队列优于双队列

```cpp
class MyStack {
    queue<int> q;
public:
    void push(int x) {
        q.push(x);
        for (int i = 0; i < q.size() - 1; i++) {  // 前面全移后面
            q.push(q.front());
            q.pop();
        }
    }
    int pop()  { int r = q.front(); q.pop(); return r; }
    int top()  { return q.front(); }
    bool empty() { return q.empty(); }
};
```

> 口诀：**新元素加队尾，前面全移后面，队首就是栈顶**

---

### 20. 有效的括号 ⭐⭐⭐

[LeetCode 20](https://leetcode.cn/problems/valid-parentheses/)

- **核心**：左括号压对应右括号，遇右括号和栈顶比，相等则弹
- **易错**：奇数长度直接 false；必须先判空再取栈顶；最后栈必须为空

```cpp
bool isValid(string s) {
    if (s.size() % 2 != 0) return false;   // 奇数必非法

    stack<char> st;
    for (char c : s) {
        if (c == '(')      st.push(')');
        else if (c == '[') st.push(']');
        else if (c == '{') st.push('}');
        else {
            if (st.empty() || st.top() != c) return false;  // 先判空！
            st.pop();
        }
    }
    return st.empty();  // 栈空才有效
}
```

> 口诀：**左压右，右匹配，空栈直接错，最后栈空才是对**

---

### 1047. 删除字符串中的所有相邻重复项 ⭐⭐

[LeetCode 1047](https://leetcode.cn/problems/remove-all-adjacent-duplicates-in-string/)

- **核心**：和括号匹配完全一致——遇栈顶相同就弹，不同就压
- **易错**：用 `string` 当栈最优（省去 reverse 步骤）；必须先判空

```cpp
string removeDuplicates(string s) {
    string res;                          // 直接用 string 当栈
    for (char c : s) {
        if (!res.empty() && res.back() == c)
            res.pop_back();              // 相同 → 弹
        else
            res.push_back(c);            // 不同 → 压
    }
    return res;
}
```

> 口诀：**相同就弹出，不同就压入，string 当栈更方便**

---

### 150. 逆波兰表达式求值 ⭐⭐

[LeetCode 150](https://leetcode.cn/problems/evaluate-reverse-polish-notation/)

- **核心**：数字压栈，运算符弹两个——**先弹右操作数，后弹左操作数**
- **易错**：减法和除法顺序不能反；`token` 是 string，用双引号比较；负数用 `stoi` 处理

```cpp
int evalRPN(vector<string>& tokens) {
    stack<int> st;
    for (string& token : tokens) {
        if (token == "+" || token == "-" || token == "*" || token == "/") {
            int right = st.top(); st.pop();  // 先弹 → 右操作数
            int left  = st.top(); st.pop();  // 后弹 → 左操作数

            if (token == "+")      st.push(left + right);
            else if (token == "-") st.push(left - right);
            else if (token == "*") st.push(left * right);
            else                   st.push(left / right);  // C++ 自动向零截断
        } else {
            st.push(stoi(token));              // 正负数通用
        }
    }
    return st.top();
}
```

> 口诀：**数字压栈，运算符弹两个，先弹右后弹左，左在前右在后**

---

### 239. 滑动窗口最大值 ⭐⭐⭐

[LeetCode 239](https://leetcode.cn/problems/sliding-window-maximum/)

- **核心**：单调递减双端队列，存下标（不是值！），队首永远是当前窗口最大值
- **易错**：必须存下标才能判断是否滑出窗口；入队用 `>=` 弹掉相等值；窗口形成后才记录结果

```cpp
vector<int> maxSlidingWindow(vector<int>& nums, int k) {
    deque<int> dq;                       // 存下标，单调递减
    vector<int> res;

    for (int i = 0; i < nums.size(); i++) {
        // ① 入队：弹掉队尾所有 ≤ 新元素的值
        while (!dq.empty() && nums[i] >= nums[dq.back()])
            dq.pop_back();
        dq.push_back(i);

        // ② 出队：队首滑出窗口则移除
        if (dq.front() <= i - k)
            dq.pop_front();

        // ③ 窗口形成，记录最大值
        if (i >= k - 1)
            res.push_back(nums[dq.front()]);
    }
    return res;
}
```

> 口诀：**入队弹小的，出队弹老的，窗口形成取队首**

---

## 三、题频排行

| 题号 | 题目 | 频率 | 难度 |
|------|------|------|------|
| 20 | 有效的括号 | ⭐⭐⭐⭐⭐ | 简单 |
| 239 | 滑动窗口最大值 | ⭐⭐⭐⭐ | 困难 |
| 150 | 逆波兰表达式求值 | ⭐⭐⭐ | 中等 |
| 1047 | 删除相邻重复项 | ⭐⭐⭐ | 简单 |
| 232 | 用栈实现队列 | ⭐⭐ | 简单 |
| 225 | 用队列实现栈 | ⭐⭐ | 简单 |

---

## 四、通用解题步骤

```
① 识别问题类型：匹配？顺序？滑动窗口？
② 选数据结构：匹配 → 栈 / 顺序 → 队列 / 窗口最值 → 单调队列
③ 写边界条件：先判空再取栈顶 / 队首
④ 验证极端输入：空 / 单元素 / 全相同
```

---

## 五、考前一分钟速记

```
栈题铁律     →  先判空，再取顶
括号匹配     →  左压右，右匹配，空栈错，栈空对
逆波兰       →  先弹右，后弹左，左在前右在后
滑动窗口     →  入队弹小的，出队弹老的，队首即最大
栈→队列     →  输出空了才倒
队列→栈     →  压完前面全移后
相邻去重     →  string 当栈，同弹异压
```

---

> **状态**：栈与队列板块 ✅ 锁死 | **下一板块**：二叉树
