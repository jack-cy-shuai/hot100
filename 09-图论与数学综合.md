# 图论 + 数学 + 字符串综合 · 机试核心速记手册

> 今日覆盖：7 题 | DFS 连通块 / 拓扑排序 / 整数运算 / 字符串匹配 / 哈希分组

---

## 一、本模块四合一概述

| 子模块 | 题数 | 核心技巧 | 代表题 |
|--------|------|---------|--------|
| 图论 | 2 | DFS 染色 / BFS 拓扑排序 | 200 / 207 |
| 数学 | 2 | 取余整除 / 半段反转 | 7 / 9 |
| 字符串 | 2 | 前缀截取 / 双指针匹配 | 14 / 28 |
| 哈希 | 1 | 排序 key 分组 | 49 |

---

## 二、7 题逐题复盘

### 第一部分：图论（2 题）

---

#### 200. 岛屿数量 ⭐⭐⭐

[LeetCode 200](https://leetcode.cn/problems/number-of-islands/)

- **核心**：DFS 原地沉岛——遇到 `'1'` 计数 +1，然后把整个岛沉为 `'0'`
- **思路**：遍历网格 → 遇到陆地 → `count++` → DFS 把连通块全部标记
- **易错**：递归前先判越界 + 是否为 `'0'`；方向数组别写错

```cpp
class Solution {
    int dx[4] = {-1, 1, 0, 0};
    int dy[4] = {0, 0, -1, 1};
    int m, n;

    void dfs(vector<vector<char>>& grid, int x, int y) {
        if (x < 0 || x >= m || y < 0 || y >= n || grid[x][y] == '0') return;
        grid[x][y] = '0';                           // 沉岛：标记已访问
        for (int i = 0; i < 4; i++)
            dfs(grid, x + dx[i], y + dy[i]);
    }

public:
    int numIslands(vector<vector<char>>& grid) {
        m = grid.size(), n = grid[0].size();
        int count = 0;
        for (int i = 0; i < m; i++)
            for (int j = 0; j < n; j++)
                if (grid[i][j] == '1') {
                    count++;
                    dfs(grid, i, j);               // 沉掉整个岛
                }
        return count;
    }
};
```

> BFS 备选：用 `queue<pair<int,int>>` 代替递归，适合极端深图防栈溢出

> 口诀：**遇岛计数加一，DFS 沉到底；上下左右全搜完，陆地变水不留迹**

---

#### 207. 课程表 ⭐⭐⭐

[LeetCode 207](https://leetcode.cn/problems/course-schedule/)

- **核心**：拓扑排序判环——建邻接表 + 入度表，入度为 0 的入队，最后看是否所有节点都访问了
- **易错**：有向边方向——`prerequisites[i] = [a, b]` 表示 `b→a`（先修 b 再修 a）

```
拓扑排序三部曲：
① 建邻接表 + 统计入度
② 入度 0 的节点入队
③ BFS 出队，更新邻接节点入度，入度变为 0 则入队
   最后 访问数 == 总课程数 → 无环
```

```cpp
bool canFinish(int numCourses, vector<vector<int>>& prerequisites) {
    vector<vector<int>> graph(numCourses);
    vector<int> inDegree(numCourses, 0);

    for (auto& p : prerequisites) {
        graph[p[1]].push_back(p[0]);               // b → a
        inDegree[p[0]]++;
    }

    queue<int> q;
    for (int i = 0; i < numCourses; i++)
        if (inDegree[i] == 0) q.push(i);

    int visited = 0;
    while (!q.empty()) {
        int cur = q.front(); q.pop();
        visited++;
        for (int next : graph[cur])
            if (--inDegree[next] == 0) q.push(next);
    }
    return visited == numCourses;                  // 全访问 = 无环
}
```

> 口诀：**入度为零先进队，出队访问减邻居；全部访问则无环，剩下一看有循环**

---

### 第二部分：数学（2 题）

---

#### 7. 整数反转 ⭐⭐

[LeetCode 7](https://leetcode.cn/problems/reverse-integer/)

- **核心**：`res = res*10 + x%10; x /= 10;`，用 `long long` 防溢出
- **易错**：返回值必须在 `[INT_MIN, INT_MAX]` 范围内

```cpp
int reverse(int x) {
    long long res = 0;                             // 🔑 long long 防溢出
    while (x) {
        res = res * 10 + x % 10;
        x /= 10;
    }
    return (res < INT_MIN || res > INT_MAX) ? 0 : (int)res;
}
```

> 口诀：**取余拼新数，原数除以十；long long 中间存，溢出返回零**

---

#### 9. 回文数 ⭐⭐

[LeetCode 9](https://leetcode.cn/problems/palindrome-number/)

- **核心**：反转后半段数字，与前半段比较——避免溢出且无需字符串
- **易错**：负数直接 false；末位为 0 且 ≠0 的数（如 10）也 false；奇数位最后 `rev/10` 比较

```cpp
bool isPalindrome(int x) {
    if (x < 0 || (x % 10 == 0 && x != 0)) return false;
    int rev = 0;
    while (x > rev) {                              // 只反转一半
        rev = rev * 10 + x % 10;
        x /= 10;
    }
    return x == rev || x == rev / 10;              // 偶数位 | 奇数位
}
```

> 口诀：**负数零尾先排除，反转一半比一比；x 和 rev 同步走，奇偶判断别忘记**

---

### 第三部分：字符串（2 题）

---

#### 14. 最长公共前缀 ⭐

[LeetCode 14](https://leetcode.cn/problems/longest-common-prefix/)

- **核心**：横向扫描——以第一个串为基准，逐个缩短直到匹配所有串
- **关键函数**：`strs[i].find(prefix) == 0` 判断 prefix 是否为前缀

```cpp
string longestCommonPrefix(vector<string>& strs) {
    if (strs.empty()) return "";
    string prefix = strs[0];
    for (int i = 1; i < strs.size(); i++) {
        while (strs[i].find(prefix) != 0) {        // 不是前缀就缩短
            prefix = prefix.substr(0, prefix.size() - 1);
            if (prefix.empty()) return "";
        }
    }
    return prefix;
}
```

> 口诀：**第一个串当基准，不是前缀就缩短；缩到为空返回空，缩到匹配继续走**

---

#### 28. 实现 strStr() ⭐

[LeetCode 28](https://leetcode.cn/problems/find-the-index-of-the-first-occurrence-in-a-string/)

- **核心**：暴力双指针匹配——外层枚举起点，内层逐字符比对
- **易错**：外层 `i <= n-m` 防越界；needle 为空返回 0

```cpp
int strStr(string haystack, string needle) {
    int n = haystack.size(), m = needle.size();
    if (m == 0) return 0;
    for (int i = 0; i <= n - m; i++) {             // 🔑 边界是 n-m
        int j = 0;
        while (j < m && haystack[i + j] == needle[j]) j++;
        if (j == m) return i;
    }
    return -1;
}
```

> KMP 进阶：时间复杂度 O(n+m)，手写困难时可先用暴力法保底

> 口诀：**外层枚举起点，内层逐位比对；全部匹配返下标，都不匹配返负一**

---

### 第四部分：哈希（1 题）

---

#### 49. 字母异位词分组 ⭐

[LeetCode 49](https://leetcode.cn/problems/group-anagrams/)

- **核心**：排序后的字符串作为 key，同一 key 的原始串归为一组
- **思路**：异位词 → 排序后完全一样 → 天然哈希 key

```cpp
vector<vector<string>> groupAnagrams(vector<string>& strs) {
    unordered_map<string, vector<string>> mp;
    for (string& s : strs) {
        string key = s;
        sort(key.begin(), key.end());              // 排序作为 key
        mp[key].push_back(s);
    }
    vector<vector<string>> res;
    for (auto& p : mp) res.push_back(p.second);
    return res;
}
```

> 备选方案：用字符计数数组作为 key（如 `"a2b1c3"`），省去排序的 O(k log k)

> 口诀：**排序当 key，原串塞进去；异位词同排序，自动分好组**

---

## 三、机试频率排行

| 题号 | 题目 | 频率 | 难度 | 模块 |
|------|------|------|------|------|
| 200 | 岛屿数量 | ⭐⭐⭐⭐⭐ | 中等 | 图论 |
| 207 | 课程表 | ⭐⭐⭐⭐ | 中等 | 图论 |
| 49 | 字母异位词分组 | ⭐⭐⭐⭐ | 中等 | 哈希 |
| 14 | 最长公共前缀 | ⭐⭐⭐ | 简单 | 字符串 |
| 7 | 整数反转 | ⭐⭐⭐ | 中等 | 数学 |
| 9 | 回文数 | ⭐⭐⭐ | 简单 | 数学 |
| 28 | strStr() | ⭐⭐ | 简单 | 字符串 |

---

## 四、考题模式速查

| 题型 | 核心解法 | 关键点 |
|------|---------|--------|
| 连通块计数 | DFS 沉岛 / BFS | 原地标记，方向数组 |
| 有向图判环 | Kahn 拓扑排序 | 入度表 + 队列 |
| 整数溢出题 | `long long` 中间存 | 最后判 INT_MIN/MAX |
| 回文判断 | 反转一半 | 奇偶位分别处理 |
| 公共前缀 | 横向/纵向扫描 | `find()==0` 判前缀 |
| 子串匹配 | 暴力双指针 / KMP | 外层边界 n-m |
| 异位词分组 | 排序 key / 计数 key | `unordered_map` 聚合 |

---

## 五、考前一分钟速记

```
岛屿数量   →  DFS 沉岛，遇 1 计数加一全淹没
课程表     →  拓扑排序，入度为零队里走
整数反转   →  long long 存，取余拼数判溢出
回文数     →  反转一半，奇偶两位分别比
最长前缀   →  首个当基准，不是前缀就截短
strStr     →  枚举起点逐位对，全匹配了返下标
异位词     →  排序为 key，哈希分组自动分
```

---

> **🎉 状态**：HOT 100 全部板块 ✅ 锁死 | 9 个文件全部完成
