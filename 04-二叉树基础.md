# 二叉树基础 · 机试核心速记手册

> 今日覆盖：12 题 | 前中后序 / 层序 / 递归三要素 | 机试占比 30%~50%

---

## 一、本板块核心框架

```
二叉树题 = 遍历 + 处理时机

├── DFS（深度优先）→ 栈 / 递归
│   ├── 前序：根左右 → 路径、翻转、构造
│   ├── 中序：左根右 → BST 相关
│   └── 后序：左右根 → 高度、深度、平衡
│
└── BFS（广度优先）→ 队列
    └── 层序：逐层处理 → 层序、最短路径、右视图
```

---

## 二、三大万能模板（必背）

### 模板 1：递归 DFS

```cpp
void dfs(TreeNode* cur, ...) {
    if (cur == nullptr) return;        // ① 终止条件

    // 【前序位置】处理当前节点
    dfs(cur->left);
    // 【中序位置】处理当前节点
    dfs(cur->right);
    // 【后序位置】处理当前节点
}
```

### 模板 2：迭代前/后序遍历

```cpp
// 前序：根左右
stack<TreeNode*> st; st.push(root);
while (!st.empty()) {
    TreeNode* cur = st.top(); st.pop();
    res.push_back(cur->val);
    if (cur->right) st.push(cur->right);  // 先右
    if (cur->left)  st.push(cur->left);   // 后左 → 出栈左先右后
}

// 后序：前序改入栈顺序（先左后右）→ 结果 reverse
// 前序(根左右) → 改入栈得(根右左) → reverse → (左右根) = 后序
```

### 模板 3：层序遍历（BFS）

```cpp
queue<TreeNode*> q; q.push(root);
while (!q.empty()) {
    int size = q.size();                 // 🔑 灵魂：记录当前层大小
    for (int i = 0; i < size; i++) {
        TreeNode* cur = q.front(); q.pop();
        // 处理 cur
        if (cur->left)  q.push(cur->left);
        if (cur->right) q.push(cur->right);
    }
    // 一层结束
}
```

---

## 三、12 题逐题复盘

### 第一阶段：遍历基础（4 题）

---

#### 144. 二叉树的前序遍历 ⭐⭐⭐

[LeetCode 144](https://leetcode.cn/problems/binary-tree-preorder-traversal/)

- **核心**：根 → 左 → 右。迭代用栈，**先压右再压左**
- **易错**：迭代入栈顺序不能反；空树直接返回空数组

```cpp
// 递归（机试首选）
void dfs(TreeNode* cur, vector<int>& res) {
    if (!cur) return;
    res.push_back(cur->val);      // 根
    dfs(cur->left, res);          // 左
    dfs(cur->right, res);         // 右
}

// 迭代（面试重点）
stack<TreeNode*> st; st.push(root);
while (!st.empty()) {
    TreeNode* cur = st.top(); st.pop();
    res.push_back(cur->val);
    if (cur->right) st.push(cur->right);  // 先右
    if (cur->left)  st.push(cur->left);   // 后左
}
```

> 口�诀�：**前序根左右，递归最简单；迭代栈实现，右左先入栈**

---

#### 94. 二叉树的中序遍历 ⭐⭐⭐

[LeetCode 94](https://leetcode.cn/problems/binary-tree-inorder-traversal/)

- **核心**：左 → 根 → 右。迭代用指针一路向左，弹栈时处理
- **易错**：循环条件 `cur || !st.empty()`；处理时机是弹栈时不是压栈时

```cpp
// 递归
void dfs(TreeNode* cur, vector<int>& res) {
    if (!cur) return;
    dfs(cur->left, res);          // 左
    res.push_back(cur->val);      // 根
    dfs(cur->right, res);         // 右
}

// 迭代
TreeNode* cur = root;
stack<TreeNode*> st;
while (cur || !st.empty()) {
    if (cur) {
        st.push(cur);
        cur = cur->left;           // 一路向左
    } else {
        cur = st.top(); st.pop();
        res.push_back(cur->val);   // 弹栈时处理
        cur = cur->right;
    }
}
```

> 口诀：**中序左根右，迭代用指针；一路向左走，弹栈才处理**

---

#### 145. 二叉树的后序遍历 ⭐⭐⭐

[LeetCode 145](https://leetcode.cn/problems/binary-tree-postorder-traversal/)

- **核心**：左 → 右 → 根。迭代 = 前序变形 → reverse
- **易错**：迭代版忘记 reverse；入栈先左后右（和前序相反）

```cpp
// 递归
void dfs(TreeNode* cur, vector<int>& res) {
    if (!cur) return;
    dfs(cur->left, res);          // 左
    dfs(cur->right, res);         // 右
    res.push_back(cur->val);      // 根
}

// 迭代：前序变形 + reverse（最巧妙）
stack<TreeNode*> st; st.push(root);
while (!st.empty()) {
    TreeNode* cur = st.top(); st.pop();
    res.push_back(cur->val);
    if (cur->left)  st.push(cur->left);   // 先左（和前序相反）
    if (cur->right) st.push(cur->right);  // 后右
}
reverse(res.begin(), res.end());           // 根右左 → 左右根
```

> 口诀：**后序左右根，递归移最后；迭代改前序，结果反转就搞定**

---

#### 102. 二叉树的层序遍历 ⭐⭐⭐

[LeetCode 102](https://leetcode.cn/problems/binary-tree-level-order-traversal/)

- **核心**：队列 + 每层预先记录 `size`（所有层序变种题的母题）
- **易错**：循环里用 `q.size()` 而不是提前存的 `size`，会动态变化导致出错

```cpp
queue<TreeNode*> q; q.push(root);
while (!q.empty()) {
    int size = q.size();               // 🔑 记录本层节点数
    vector<int> level;
    for (int i = 0; i < size; i++) {
        TreeNode* cur = q.front(); q.pop();
        level.push_back(cur->val);
        if (cur->left)  q.push(cur->left);
        if (cur->right) q.push(cur->right);
    }
    res.push_back(level);
}
```

> 口诀：**层序遍历用队列，根先入队；每层先记大小，遍历完再下�一层**

---

### 第二阶段：遍历应用（5 题）

---

#### 226. 翻转二叉树 ⭐⭐

[LeetCode 226](https://leetcode.cn/problems/invert-binary-tree/)

- **核心**：每个节点 `swap` 左右子树 → 递归翻转
- **易错**：**不能用中序！** 中序会导致部分节点被交换两次

```cpp
TreeNode* invertTree(TreeNode* root) {
    if (!root) return nullptr;
    swap(root->left, root->right);   // 前序位置交换
    invertTree(root->left);
    invertTree(root->right);
    return root;
}
```

> 口诀：**翻转二叉树，每个节点左右换；前序后序都可以，中序绝对不能干**

---

#### 101. 对称二叉树 ⭐⭐⭐

[LeetCode 101](https://leetcode.cn/problems/symmetric-tree/)

- **核心**：比较左右子树是否镜像，遵循 **外对外、内对内**
- **易错**：终止条件顺序不能乱（都空 → 一个空 → 值不等）

```cpp
bool compare(TreeNode* L, TreeNode* R) {
    if (!L && !R) return true;          // 都空 → 对称
    if (!L || !R) return false;         // 一个空 → 不对称
    if (L->val != R->val) return false; // 值不同 → 不对称

    return compare(L->left, R->right)   // 外：左左 vs 右右
        && compare(L->right, R->left);  // 内：左右 vs 右左
}
```

> 口诀：**对称二叉树，左右子树比；外对外内对内，都空才是对**

---

#### 104. 二叉树的最大深度 ⭐⭐⭐

[LeetCode 104](https://leetcode.cn/problems/maximum-depth-of-binary-tree/)

- **核心**：后序，左右取 max + 1
- **易错**：别忘了 +1

```cpp
int maxDepth(TreeNode* root) {
    if (!root) return 0;
    return max(maxDepth(root->left), maxDepth(root->right)) + 1;
}
```

> 口诀：**最大深度后序算，左右取大加个1**

---

#### 111. 二叉树的最小深度 ⭐⭐

[LeetCode 111](https://leetcode.cn/problems/minimum-depth-of-binary-tree/)

- **核心**：不能简单把 max 改成 min！单侧子树为空时取另一侧
- **易错**：叶子节点 = 左右都空；层序找到第一个叶子直接返回

```cpp
int minDepth(TreeNode* root) {
    if (!root) return 0;
    int L = minDepth(root->left);
    int R = minDepth(root->right);
    // 单侧为空 → 取另一侧 + 1
    if (!root->left)  return R + 1;
    if (!root->right) return L + 1;
    return min(L, R) + 1;              // 两侧都有 → 取 min
}
```

> 口诀：**最小深度不简单，不能直接改 min；单侧为空取另一侧，层序找到第一个叶**

---

#### 222. 完全二叉树的节点个数 ⭐

[LeetCode 222](https://leetcode.cn/problems/count-complete-tree-nodes/)

- **核心**：利用完全二叉树性质——左右高度相等 → 满二叉树公式 `2^h-1`
- **易错**：C++ 中 `1 << h` 表示 `2^h`，不是 `^`

```cpp
int countNodes(TreeNode* root) {
    if (!root) return 0;
    // 算左/右边界高度
    int lh = 0, rh = 0;
    TreeNode *l = root->left, *r = root->right;
    while (l) { lh++; l = l->left; }
    while (r) { rh++; r = r->right; }

    if (lh == rh) return (1 << (lh + 1)) - 1;  // 满二叉树
    return countNodes(root->left) + countNodes(root->right) + 1;
}
```

> 口诀：**完全二叉树节点数，左右高度比一比；相等就是满二叉，公式直接算个数**

---

### 第三阶段：进阶应用（3 题）

---

#### 110. 平衡二叉树 ⭐⭐

[LeetCode 110](https://leetcode.cn/problems/balanced-binary-tree/)

- **核心**：后序算高度，用 `-1` 标记不平衡，**提前返回**避免无效计算
- **易错**：暴力法 O(n²)；最优解发现不平衡立刻向上返回 -1

```cpp
int getHeight(TreeNode* node) {
    if (!node) return 0;
    int L = getHeight(node->left);
    if (L == -1) return -1;            // 提前返回
    int R = getHeight(node->right);
    if (R == -1) return -1;
    if (abs(L - R) > 1) return -1;     // 不平衡
    return max(L, R) + 1;              // 平衡，返回高度
}
bool isBalanced(TreeNode* root) { return getHeight(root) != -1; }
```

> 口诀：**平衡二叉树，后序算高度；差超 1 返 -1，提前返回效率高**

---

#### 257. 二叉树的所有路径 ⭐⭐

[LeetCode 257](https://leetcode.cn/problems/binary-tree-paths/)

- **核心**：前序 + 隐式回溯。传 `path` 值拷贝，自动回溯
- **易错**：必须前序；只在非叶子后加 `->`

```cpp
void dfs(TreeNode* cur, string path, vector<string>& res) {
    path += to_string(cur->val);
    if (!cur->left && !cur->right) {   // 叶子 → 存结果
        res.push_back(path);
        return;
    }
    if (cur->left)  dfs(cur->left,  path + "->", res);
    if (cur->right) dfs(cur->right, path + "->", res);
}
```

> 口诀：**前序遍历记路径，遇到叶子存结果；值传 path 隐回溯，非叶才加箭头符**

---

#### 404. 左叶子之和 ⭐

[LeetCode 404](https://leetcode.cn/problems/sum-of-left-leaves/)

- **核心**：**只能通过父节点判断**左叶子——`root->left` 存在且是叶子
- **易错**：不能从自身判断是否左叶子；右子树里也可能有左叶子

```cpp
int sumOfLeftLeaves(TreeNode* root) {
    if (!root) return 0;
    int sum = 0;
    // 父节点视角：左孩子是叶子 → 累加
    if (root->left && !root->left->left && !root->left->right)
        sum += root->left->val;
    return sum + sumOfLeftLeaves(root->left) + sumOfLeftLeaves(root->right);
}
```

> 口诀：**左叶子之和，父节点来判；左孩子是叶子，就把值加算**

---

## 四、遍历选择速查表

| 题目类型 | 推荐遍历 | 原因 |
|----------|----------|------|
| 翻转二叉树 | 前序 / 后序 | 先交换再递归 |
| 对称二叉树 | 后序 | 先知道子树结果再合并 |
| 最大/最小深度 | 后序 | 先知道子树高度 |
| 平衡二叉树 | 后序 | 先知道子树是否平衡 |
| 所有路径 | 前序 | 路径从根开始 |
| 左叶子之和 | 任意 | 需要父节点视角 |
| 层序相关 | BFS | 按层处理 |
| 最短路径 | BFS | 首次遇到叶子即最短 |

---

## 五、高频考点排行

| 题号 | 题目 | 频率 | 难度 |
|------|------|------|------|
| 226 | 翻转二叉树 | ⭐⭐⭐⭐⭐ | 简单 |
| 101 | 对称二叉树 | ⭐⭐⭐⭐ | 简单 |
| 104 | 二叉树最大深度 | ⭐⭐⭐⭐ | 简单 |
| 102 | 层序遍历 | ⭐⭐⭐⭐ | 中等 |
| 110 | 平衡二叉树 | ⭐⭐⭐ | 简单 |
| 111 | 二叉树最小深度 | ⭐⭐⭐ | 简单 |
| 257 | 所有路径 | ⭐⭐⭐ | 简单 |
| 144/94/145 | 三种遍历 | ⭐⭐⭐ | 简单 |
| 404 | 左叶子之和 | ⭐⭐ | 简单 |
| 222 | 完全二叉树节点数 | ⭐⭐ | 中等 |

---

## 六、考前一分钟速记

```
递归三要素     →  终止条件 + 单层逻辑 + 返回值
遍历位置       →  前（路径）中（BST）后（高度/深度）
层序遍历灵魂   →  先记 size，再 for 循环
翻转二叉树     →  swap 左右，绝不用中序
对称二叉树     →  外对外，内对内
最大/小深度    →  后序 max+1 / 单侧空取另一侧
平衡二叉树     →  后序用 -1 提前返回
左叶子之和     →  父节点视角判断
```

---

> **状态**：二叉树基础板块 ✅ 锁死 | **下一板块**：二叉树进阶 / 贪心
