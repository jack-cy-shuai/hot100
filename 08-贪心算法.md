# 贪心算法 · 机试核心速记手册

> 今日覆盖：7 题 | 排序匹配 / 跳跃区间 / 股票最值 / 数学调度 | O(n) 高效秒杀

---

## 一、贪心核心思想

```
每一步只做局部最优选择，不回溯不预判
→ 连续的局部最优 → 全局最优

适用前提：问题具备无后效性（当前选择不影响后续决策）
```

### 四大题型套路

| 题型 | 核心策略 | 代表题 |
|------|---------|--------|
| 分配匹配 | 排序 + 双指针 | 455 |
| 跳跃区间 | 维护最远可达 / 区间边界 | 55 / 45 |
| 最值舍弃 | 遍历更新最值，主动舍负收益 | 53 / 121 / 122 |
| 统计调度 | 频次统计 + 数学公式 | 621 |

---

## 二、7 题逐题复盘

---

### 455. 分发饼干 ⭐

[LeetCode 455](https://leetcode.cn/problems/assign-cookies/)

- **核心**：排序 + 双指针，最小饼干喂最小胃口
- **易错**：循环条件两个指针都要判；匹配成功才同时移，否则只移饼干

```cpp
int findContentChildren(vector<int>& g, vector<int>& s) {
    sort(g.begin(), g.end());
    sort(s.begin(), s.end());
    int i = 0, j = 0;
    while (i < g.size() && j < s.size()) {
        if (g[i] <= s[j]) i++;    // 喂饱，孩子+1
        j++;                       // 饼干总是消耗
    }
    return i;
}
```

> 口诀：**排序双指针，最小喂最小；喂饱一起走，不饱饼干丢**

---

### 55. 跳跃游戏 ⭐⭐

[LeetCode 55](https://leetcode.cn/problems/jump-game/)

- **核心**：维护 `maxReach`，遍历可达范围更新最远距离
- **易错**：`cur <= maxReach` 不可省略；`n==1` 直接 true

```cpp
bool canJump(vector<int>& nums) {
    int n = nums.size();
    if (n == 1) return true;
    int maxReach = nums[0];
    for (int i = 1; i <= maxReach && i < n; i++) {
        maxReach = max(maxReach, i + nums[i]);
        if (maxReach >= n - 1) return true;
    }
    return false;
}
```

> 口诀：**走一步看一步，最远距离实时更；超出边界过不去，覆盖终点就能到**

---

### 45. 跳跃游戏 II ⭐⭐

[LeetCode 45](https://leetcode.cn/problems/jump-game-ii/)

- **核心**：区间贪心——`end` 为当前区间边界，触边时跳跃次数 +1
- **易错**：只遍历到 `n-2`（最后位置不需再跳）；`n==1` 返回 0

```cpp
int jump(vector<int>& nums) {
    int n = nums.size();
    if (n == 1) return 0;
    int maxReach = 0, end = 0, steps = 0;
    for (int i = 0; i < n - 1; i++) {
        maxReach = max(maxReach, i + nums[i]);
        if (i == end) {              // 走到当前区间边界
            steps++;
            end = maxReach;          // 下一段区间的边界
            if (end >= n - 1) break;
        }
    }
    return steps;
}
```

> 口诀：**走到区间头，必须跳一步；新区间看最远，跳数加一继续走**

---

### 53. 最大子数组和 ⭐⭐⭐

[LeetCode 53](https://leetcode.cn/problems/maximum-subarray/)

- **核心**：累加过程中 `curSum < 0` 就重置为 0（负收益拖后腿，果断舍弃）
- **易错**：**顺序不能颠倒**——先累加 → 再更新 max → 最后判负重置；`maxSum` 初始 `INT_MIN` 兼容全负数

```cpp
int maxSubArray(vector<int>& nums) {
    int curSum = 0, maxSum = INT_MIN;
    for (int x : nums) {
        curSum += x;                 // ① 先累加
        maxSum = max(maxSum, curSum);// ② 更新最值
        if (curSum < 0) curSum = 0;  // ③ 负则舍弃
    }
    return maxSum;
}
```

> 口诀：**累加更新先于重置，负了清零重新开始；全负数靠 INT_MIN 兜底**

---

### 121. 买卖股票的最佳时机 ⭐⭐⭐

[LeetCode 121](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock/)

- **核心**：记录历史最低价，每天算当天卖出利润，取最大
- **易错**：`n <= 1` 返回 0；先更新最低价再算利润（不能用未来价格买）

```cpp
int maxProfit(vector<int>& prices) {
    if (prices.size() <= 1) return 0;
    int minPrice = prices[0], maxProfit = 0;
    for (int i = 1; i < prices.size(); i++) {
        maxProfit = max(maxProfit, prices[i] - minPrice);  // 今天卖
        minPrice  = min(minPrice,  prices[i]);             // 更新最低
    }
    return maxProfit;
}
```

> 口诀：**记录历史最低，每天算当日利；买卖各一次，最大利润取**

---

### 122. 买卖股票的最佳时机 II ⭐⭐

[LeetCode 122](https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-ii/)

- **核心**：只要今天比昨天贵，差价就赚——累加所有上涨段
- **易错**：`n==1` 返回 0；不限交易次数所以贪心最优

```cpp
int maxProfit(vector<int>& prices) {
    int profit = 0;
    for (int i = 1; i < prices.size(); i++)
        if (prices[i] > prices[i - 1])
            profit += prices[i] - prices[i - 1];
    return profit;
}
```

> 121 vs 122 对比：

| | 121 只买卖一次 | 122 不限买卖 |
|--|-------------|------------|
| 策略 | 记录最低价，找最大价差 | 累加所有上涨段 |
| 核心变量 | `minPrice` | 无 |
| 代码行数 | 多几行 | 一行搞定 |

> 口诀：**涨了就赚，跌了不碰；上涨差价全收割**

---

### 621. 任务调度器 ⭐⭐⭐

[LeetCode 621](https://leetcode.cn/problems/task-scheduler/)

- **核心**：桶思想——`(maxCnt - 1) × (n + 1) + sameMax`，取公式值和任务总数的较大值
- **易错**：必须 `max(公式, 总数)` 覆盖任务过多无需空闲的场景

```
桶模型（n=2, 最多任务=A 出现3次）：
┌─────────┐
│ A B C   │ ← 每桶 n+1 = 3 个槽
│ A B _   │
│ A       │ ← 最后一桶只放 maxCnt 频次的任务
└─────────┘
总时间 = (maxCnt-1) × (n+1) + sameMax
```

```cpp
int leastInterval(vector<char>& tasks, int n) {
    vector<int> cnt(26, 0);
    for (char c : tasks) cnt[c - 'A']++;

    int maxCnt  = *max_element(cnt.begin(), cnt.end());
    int sameMax = count(cnt.begin(), cnt.end(), maxCnt);

    int formula = (maxCnt - 1) * (n + 1) + sameMax;
    return max(formula, (int)tasks.size());    // 🔑 取较大值
}
```

> 口诀：**桶排任务算时间，最多任务定桶数；公式做底取最大，任务太多也容纳**

---

## 三、题型分类速查

| 题型 | 题目 | 核心操作 | 时间 | 空间 |
|------|------|---------|------|------|
| 分配匹配 | 455 分发饼干 | 排序 + 双指针 | O(nlogn) | O(1) |
| 跳跃01 | 55 能否到终点 | 维护 maxReach | O(n) | O(1) |
| 跳跃02 | 45 最少跳跃 | 区间边界 end | O(n) | O(1) |
| 子数组最值 | 53 最大子数组和 | 累加 + 负舍弃 | O(n) | O(1) |
| 股票1 | 121 一次买卖 | 记录最低价 | O(n) | O(1) |
| 股票2 | 122 不限买卖 | 累加上涨差价 | O(n) | O(1) |
| 任务调度 | 621 最少耗时 | 频次 + 桶公式 | O(n) | O(1) |

---

## 四、机试频率排行

| 题号 | 题目 | 频率 | 难度 |
|------|------|------|------|
| 53 | 最大子数组和 | ⭐⭐⭐⭐⭐ | 中等 |
| 121 | 买股票 I | ⭐⭐⭐⭐⭐ | 简单 |
| 621 | 任务调度器 | ⭐⭐⭐⭐ | 中等 |
| 122 | 买股票 II | ⭐⭐⭐⭐ | 中等 |
| 55 | 跳跃游戏 | ⭐⭐⭐ | 中等 |
| 45 | 跳跃游戏 II | ⭐⭐⭐ | 中等 |
| 455 | 分发饼干 | ⭐⭐ | 简单 |

---

## 五、高频致命坑点

| # | 坑点 | 题目 |
|---|------|------|
| 1 | **全负数数组**：`maxSum` 必须 `INT_MIN` 不能是 0 | 53 |
| 2 | **单元素数组**：跳跃 n=1 直接返回，股票 n≤1 利润=0 | 45/55/121 |
| 3 | **621 忘了取 max**：只套公式不对比任务总数 | 621 |
| 4 | **53 顺序颠倒**：先累加→再更新→最后重置，顺序不能乱 | 53 |
| 5 | **边界遍历**：跳跃 II 只遍历到 n-2 | 45 |
| 6 | **minPrice 初始值**：必须初始化为 prices[0] | 121 |

---

## 六、考前一分钟速记

```
排序匹配   →  排序 + 双指针，小的配小的
跳跃游戏   →  最远距离，触边就跳
最大子数组 →  累加更新判负，顺序不能乱
股票一次   →  记录最低价，算当日差价
股票多次   →  涨了就收，跌了不管
任务调度   →  桶公式取最大，任务太多也容纳
```

---

> **状态**：贪心算法板块 ✅ 锁死 | **下一板块**：子序列 DP
