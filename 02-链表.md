# 链表 · 机试核心速记手册

> 今日覆盖：8 题 | 虚拟头节点 / 双指针 / 临时变量保存 | 100% 必考题型

---

## 一、本板块三大核心技巧

| # | 技巧 | 适用场景 | 作用 |
|---|------|----------|------|
| 1 | **虚拟头节点（dummy）** | 头节点可能被修改/删除 | 统一操作，不用特殊处理 head |
| 2 | **双指针（快慢/交换遍历）** | 找倒数 N、找环、找中点、找相交 | 一次遍历，O(n) 时间 O(1) 空间 |
| 3 | **临时变量保存 next** | 所有修改指针的操作 | 防止断链，不丢节点 |

---

## 二、什么时候用虚拟头节点？

| ✅ 必须用 | ❌ 完全不用 |
|-----------|-------------|
| 203 移除链表元素 | 206 反转链表 |
| 707 设计链表 | 160 相交链表 |
| 24 两两交换节点 | 141 环形链表 |
| 19 删除倒数第 N 个 | 142 环形链表 II |

---

## 三、8 题逐题复盘

### 203. 移除链表元素 ⭐⭐⭐

[LeetCode 203](https://leetcode.cn/problems/remove-linked-list-elements/)

- **核心**：虚拟头节点统一处理头节点删除
- **易错**：删除节点后 `cur` 不能移动，继续检查新 next；最后返回 `dummy->next`

```cpp
ListNode* dummy = new ListNode(0);
dummy->next = head;
ListNode* cur = dummy;

while (cur->next != nullptr) {
    if (cur->next->val == val) {
        ListNode* temp = cur->next;
        cur->next = cur->next->next;
        delete temp;              // 删完不能移动 cur！
    } else {
        cur = cur->next;          // 不删才走下一步
    }
}
return dummy->next;
```

> 口诀：**虚拟头，cur 走，找到就跳过，不删才移步**

---

### 707. 设计链表 ⭐⭐

[LeetCode 707](https://leetcode.cn/problems/design-linked-list/)

- **核心**：虚拟头 + `size` 变量，封装所有增删改查
- **易错**：必须 `new` 创建节点；增删同步更新 `size`；`addAtIndex` 调用 `addAtTail` 后必须 `return`

```cpp
class MyLinkedList {
    struct ListNode { int val; ListNode* next; ListNode(int x) : val(x), next(nullptr) {} };
    ListNode* dummy;
    int size;
public:
    MyLinkedList() { dummy = new ListNode(0); size = 0; }
    // get / addAtHead / addAtTail / addAtIndex / deleteAtIndex ...
};
```

> 口诀：**虚拟头加 size，增删改查全靠它，先存后改再更新**

---

### 206. 反转链表 ⭐⭐⭐

[LeetCode 206](https://leetcode.cn/problems/reverse-linked-list/)

- **核心**：三指针迭代（`pre` / `cur` / `temp`）
- **易错**：必须先保存 `temp = cur->next` 防断链；循环结束返回 `pre`（不是 `cur`）

```cpp
ListNode* pre = nullptr;
ListNode* cur = head;

while (cur != nullptr) {
    ListNode* temp = cur->next;   // ① 存下一个
    cur->next = pre;              // ② 转指针
    pre = cur;                    // ③ pre 前移
    cur = temp;                   // ④ cur 前移
}
return pre;  // 不是 cur！
```

> 口诀：**存下一个 → 转指针 → 移 pre → 移 cur**

---

### 24. 两两交换链表中的节点 ⭐⭐

[LeetCode 24](https://leetcode.cn/problems/swap-nodes-in-pairs/)

- **核心**：虚拟头 + 四步固定交换
- **易错**：循环条件 `cur->next && cur->next->next` 顺序不能反；`cur` 每次移到 A 位置

```cpp
ListNode* dummy = new ListNode(0);
dummy->next = head;
ListNode* cur = dummy;

while (cur->next && cur->next->next) {
    ListNode* A = cur->next;
    ListNode* B = cur->next->next;
    ListNode* temp = B->next;

    cur->next = B;     // ①
    B->next = A;       // ②
    A->next = temp;    // ③
    cur = A;           // ④ cur 移到 A（即原第一个，现第二个）
}
return dummy->next;
```

> 口诀：**存 A 存 B 存 temp，cur → B → A → temp，cur 移到 A**

---

### 19. 删除链表的倒数第 N 个节点 ⭐⭐⭐

[LeetCode 19](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/)

- **核心**：虚拟头 + 快慢双指针
- **易错**：快慢都从 `dummy` 起（不是 `head`）；快先走 `n` 步（不是 n+1）；条件 `fast->next != nullptr`

```cpp
ListNode* dummy = new ListNode(0);
dummy->next = head;
ListNode* fast = dummy, *slow = dummy;

for (int i = 0; i < n; i++) fast = fast->next;   // 快先走 n 步

while (fast->next != nullptr) {                   // 同步走到 fast 到尾
    fast = fast->next;
    slow = slow->next;
}
// slow 停在倒数第 n+1 个
slow->next = slow->next->next;                    // 删除 slow 的下一个
return dummy->next;
```

> 口诀：**快先走 n 步，同步走到头，慢指前一个，删除不用愁**

---

### 160. 相交链表 ⭐⭐

[LeetCode 160](https://leetcode.cn/problems/intersection-of-two-linked-lists/)

- **核心**：双指针交换遍历（`pA` 走完跳 `headB`，`pB` 走完跳 `headA`）
- **易错**：先判空再决定跳转还是移动；不相等返回 `nullptr` 即 `pA`

```cpp
ListNode* pA = headA;
ListNode* pB = headB;

while (pA != pB) {
    pA = (pA == nullptr) ? headB : pA->next;
    pB = (pB == nullptr) ? headA : pB->next;
}
return pA;  // 相等时返回交点，不相等等于 nullptr
```

> 口诀：**你走我的路，我走你的路，有缘路口见，无缘同归 null**

---

### 141. 环形链表 ⭐⭐⭐

[LeetCode 141](https://leetcode.cn/problems/linked-list-cycle/)

- **核心**：快慢指针（龟兔赛跑），快走 2 步，慢走 1 步
- **易错**：循环条件 `fast && fast->next` 顺序不能反

```cpp
ListNode* slow = head;
ListNode* fast = head;

while (fast != nullptr && fast->next != nullptr) {
    slow = slow->next;
    fast = fast->next->next;
    if (slow == fast) return true;  // 相遇 → 有环
}
return false;  // fast 走到头 → 无环
```

> 口诀：**龟兔赛跑，快 2 慢 1，相遇有环，到头无环**

---

### 142. 环形链表 II ⭐⭐⭐

[LeetCode 142](https://leetcode.cn/problems/linked-list-cycle-ii/)

- **核心**：快慢找相遇点 → `head` 和相遇点同步走 → 相遇处即入口
- **易错**：相遇后两指针都走 1 步；不需要虚拟头

```cpp
ListNode* slow = head, *fast = head;

while (fast != nullptr && fast->next != nullptr) {
    slow = slow->next;
    fast = fast->next->next;

    if (slow == fast) {                    // ① 找到相遇点
        ListNode* p1 = head;
        ListNode* p2 = slow;
        while (p1 != p2) {                 // ② 同步走找入口
            p1 = p1->next;
            p2 = p2->next;
        }
        return p1;                         // ③ 入口
    }
}
return nullptr;
```

> 口诀：**快慢相遇，一个回头，同步再走，入口就有**

---

## 四、通用解题步骤

```
① 判断：需要虚拟头节点吗？
② 定义：cur / pre / fast / slow 各自初始化
③ 遍历：修改指针前先保存下一个节点
④ 移动：注意循环条件和指针移动顺序
⑤ 返回：dummy->next 还是 pre？想清楚
```

---

## 五、机试频率排行

| 题号 | 题目 | 频率 | 难度 |
|------|------|------|------|
| 206 | 反转链表 | ⭐⭐⭐⭐⭐ | 简单 |
| 141/142 | 环形链表 I/II | ⭐⭐⭐⭐ | 中等 |
| 19 | 删除倒数第 N 个 | ⭐⭐⭐⭐ | 中等 |
| 203 | 移除链表元素 | ⭐⭐⭐ | 简单 |
| 160 | 相交链表 | ⭐⭐⭐ | 简单 |
| 24 | 两两交换节点 | ⭐⭐ | 中等 |
| 707 | 设计链表 | ⭐⭐ | 中等 |

---

## 六、考前一分钟速记

```
虚拟头  →  头会变，就用它
双指针  →  快慢找环找倒数，交换遍历找相交
改指针  →  先存后改，顺序不乱
删节点  →  先改指针，再 delete
```

---

> **状态**：链表板块 ✅ 锁死 | **下一板块**：栈与队列
