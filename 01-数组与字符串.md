# 数组与字符串 · 机试核心速记手册

> 今日覆盖：11 题 | 双指针 / 反转 / 二分 | 考场直接默写

---

## 一、本板块四大核心思想

| # | 思想 | 适用场景 |
|---|------|----------|
| 1 | **快慢双指针** | 原地去重、移除元素、移动零 |
| 2 | **左右对撞双指针** | 回文判断、数组平方、字符串反转 |
| 3 | **区间反转** | 手写 reverse + 三次反转轮转数组，原地 O(1) |
| 4 | **基础二分** | 有序数组查找/插入，固定模板无脑套 |

---

## 二、四大通用必背模板

### 1. 快慢双指针（283 / 27 / 26 系列）

```cpp
int slow = 0;
for (int fast = 0; fast < n; fast++) {
    if (符合保留条件) {
        nums[slow++] = nums[fast];
    }
}
// slow 即为有效长度
```

> 口诀：**慢存快跑，结尾清零**

### 2. 左右对撞双指针（344 / 125 / 977）

```cpp
int l = 0, r = n - 1;
while (l < r) {
    // 两端判断 / 交换
    l++; r--;
}
```

> 口诀：**左右对撞，两头逼近**

### 3. 自定义区间反转（万能工具函数）

```cpp
void reverse(vector<int>& nums, int l, int r) {
    while (l < r) swap(nums[l++], nums[r--]);
}
```

> 189 轮转数组、字符串反转全部依赖这个，**必背**

### 4. 二分搜索插入位置（35）

```cpp
int l = 0, r = nums.size() - 1;
while (l <= r) {
    int mid = l + (r - l) / 2;       // 防溢出写法
    if (nums[mid] == target) return mid;
    else if (nums[mid] < target) l = mid + 1;
    else r = mid - 1;
}
return l;  // 找不到：l 就是插入下标
```

> 口诀：**小左大右，没找返左**

---

## 三、11 题逐题复盘

### 283. 移动零 ⭐⭐⭐
- **核心**：快慢指针留非零，末尾统一补 0
- **易错**：不要用交换，两步写法最稳（先筛非零，再补零）
- **模板**：快慢双指针

```cpp
int slow = 0;
for (int fast = 0; fast < n; fast++)
    if (nums[fast] != 0) nums[slow++] = nums[fast];
while (slow < n) nums[slow++] = 0;
```

---

### 344. 反转字符串 ⭐⭐⭐
- **核心**：左右双指针 swap 原地反转
- **易错**：必须原地操作，不可新开数组
- **模板**：左右对撞双指针

```cpp
int l = 0, r = s.size() - 1;
while (l < r) swap(s[l++], s[r--]);
```

---

### 541. 反转字符串 II ⭐⭐
- **核心**：每 `2k` 一段，反转前 `k` 个
- **易错**：末尾不足 k 时，反转边界取 `min(i + k - 1, n - 1)`

```cpp
for (int i = 0; i < n; i += 2 * k) {
    int l = i, r = min(i + k - 1, n - 1);
    while (l < r) swap(s[l++], s[r--]);
}
```

---

### 151. 反转字符串中的单词 ⭐⭐
- **核心**：去多余空格 → 整体反转 → 逐单词反转（三步走）
- **易错**：首尾空格、单词间多余空格要先清除

```
步骤：
1. 去除首尾及中间多余空格
2. 整个字符串反转
3. 逐个单词再反转回来
```

---

### 125. 验证回文串 ⭐⭐
- **核心**：`isalnum` 滤符号 + `tolower` 统一小写
- **易错**：**括号位置！** 先取值再比较，不要在自增语句里取值

```cpp
int l = 0, r = s.size() - 1;
while (l < r) {
    while (l < r && !isalnum(s[l])) l++;
    while (l < r && !isalnum(s[r])) r--;
    if (tolower(s[l]) != tolower(s[r])) return false;
    l++; r--;  // 比较完了再移动，不要边取值边自增
}
return true;
```

---

### 27. 移除元素 ⭐⭐⭐
- **核心**：快慢指针筛选，slow 即答案长度
- **易错**：无需额外计数变量
- **模板**：快慢双指针

```cpp
int slow = 0;
for (int fast = 0; fast < n; fast++)
    if (nums[fast] != val) nums[slow++] = nums[fast];
return slow;
```

---

### 26. 删除有序数组中的重复项 ⭐⭐⭐
- **核心**：有序相邻比对，slow 从 1 开始
- **易错**：只保留有序不重复元素，第一个元素必保留

```cpp
int slow = 1;
for (int fast = 1; fast < n; fast++)
    if (nums[fast] != nums[fast - 1])
        nums[slow++] = nums[fast];
return slow;
```

---

### 88. 合并两个有序数组 ⭐⭐
- **核心**：逆序双指针，从后往前填
- **易错**：正序会覆盖 nums1 原有数据，**必须倒着合并**

```cpp
int p1 = m - 1, p2 = n - 1, tail = m + n - 1;
while (p2 >= 0) {
    if (p1 >= 0 && nums1[p1] > nums2[p2])
        nums1[tail--] = nums1[p1--];
    else
        nums1[tail--] = nums2[p2--];
}
```

---

### 977. 有序数组的平方 ⭐⭐
- **核心**：双指针从两端取较大值，从后往前填
- **易错**：禁止一边取值一边自增指针；两端的绝对值更大者先入结果

```cpp
vector<int> res(n);
int l = 0, r = n - 1, pos = n - 1;
while (l <= r) {
    if (abs(nums[l]) > abs(nums[r]))
        res[pos--] = nums[l] * nums[l++];
    else
        res[pos--] = nums[r] * nums[r--];
}
return res;
```

---

### 189. 轮转数组 ⭐
- **考场首选（O(1) 空间）**：三次反转
  - 整体反转 → 反转前 k → 反转后 n-k
- **简单版**：额外数组拼接（O(n) 空间，够用就写这个）
- **必做**：`k %= n`，防止 k 大于 n

```cpp
// 三次反转版（原地 O(1)）
k %= n;
reverse(nums, 0, n - 1);       // 整体反转
reverse(nums, 0, k - 1);       // 前 k 个反转
reverse(nums, k, n - 1);       // 后半反转
```

> 口诀：**全翻 → 前 k 翻 → 后半翻**

---

### 35. 搜索插入位置 ⭐
- **核心**：经典二分，找不到返回左指针 `l`
- **易错**：循环条件 `l <= r` 不能写成 `<`

```cpp
int l = 0, r = nums.size() - 1;
while (l <= r) {
    int mid = l + (r - l) / 2;
    if (nums[mid] == target) return mid;
    else if (nums[mid] < target) l = mid + 1;
    else r = mid - 1;
}
return l;
```

---

## 四、高频致命坑点

| # | 坑点 | 说明 |
|---|------|------|
| 1 | **指针取值顺序** | 先取值，再移动；禁止一行内取值+自增 |
| 2 | **轮转 k 取模** | `k %= n` 必写，否则 k > n 时溢出 |
| 3 | **字符串过滤** | 字母数字过滤 `isalnum`、大小写统一 `tolower` 是默认考点 |
| 4 | **原地操作** | 不能新开数组时，优先双指针 / 反转 |
| 5 | **二分边界** | `l <= r` 不能写 `<`；mid 用 `l+(r-l)/2` 防溢出；找不到返 `l` |

---

## 五、极简口诀汇总

```
筛选数组  → 慢存快跑，结尾清零
对称反转  → 左右对撞，两头逼近
数组轮转  → 全翻 → 前k翻 → 后半翻
二分查找  → 小左大右，没找返左
合并有序  → 从后往前，大的先落位
```

---

> **状态**：数组 & 字符串板块 ✅ 锁死 | **下一板块**：链表
