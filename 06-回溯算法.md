# 回溯算法 · 机试核心速记手册

> 今日覆盖：11 题 | 组合 / 子集 / 排列 / 分割 / 棋盘 | 万能模板一招破万法

---

## 一、回溯 = 递归 + 撤销选择

```
回溯本质：带剪枝的暴力搜索，系统遍历所有解空间

核心三步：
  ① 做选择：path.push_back(元素)
  ② 递归进入下一层
  ③ 撤销选择（回溯）：path.pop_back()
```

| 问题类型 | 控制方式 | 时间 | 代表题 |
|----------|---------|------|--------|
| 组合 | `startIndex` | O(2ⁿ) | 77 / 216 / 39 / 40 |
| 子集 | `startIndex` | O(2ⁿ) | 78 / 90 |
| 排列 | `used` 数组 | O(n!) | 46 / 47 |
| 分割 | `startIndex` | O(2ⁿ) | 131 / 93 |
| 棋盘 | 合法性检查 | O(n!) | 51 |

---

## 二、回溯万能模板（必背）

```cpp
vector<ResultType> res;
vector<PathType>   path;
// 排列题才加：vector<bool> used;

void backtrack(参数) {
    if (终止条件) {          // ① 终止：叶子/路径满/分割完
        res.push_back(path);
        return;
    }

    for (int i = 起始; i < 范围; i++) {
        if (剪枝条件) continue;    // ② 剪枝

        // ③ 做选择
        path.push_back(元素);
        // 排列题额外：used[i] = true;

        backtrack(更新参数);       // ④ 递归

        // ⑤ 撤销（回溯）
        path.pop_back();
        // 排列题额外：used[i] = false;
    }
}
```

---

## 三、11 题逐题复盘

### 第一阶段：组合问题（4 题）

---

#### 77. 组合 ⭐⭐⭐

[LeetCode 77](https://leetcode.cn/problems/combinations/)

- **核心**：`startIndex` 防止重复；`path.size() == k` 时收集
- **易错**：递归传 `i + 1` 不是 `startIndex + 1`；剪枝 `i <= n - (k - path.size()) + 1`

```cpp
void backtrack(int n, int k, int start) {
    if (path.size() == k) { res.push_back(path); return; }
    for (int i = start; i <= n - (k - path.size()) + 1; i++) {
        path.push_back(i);
        backtrack(n, k, i + 1);          // 传 i+1，元素不重复
        path.pop_back();
    }
}
```

> 口诀：**组合用 start，路径够长加，递归传 i+1，剪枝提效率**

---

#### 216. 组合总和 III ⭐⭐

[LeetCode 216](https://leetcode.cn/problems/combination-sum-iii/)

- **核心**：77 的变形，加一个和条件——`path.size() == k` 时判 `curSum == n`
- **易错**：同时更新 `path` 和 `curSum`，一个不能漏

```cpp
void backtrack(int k, int n, int start) {
    if (path.size() == k) {
        if (curSum == n) res.push_back(path);
        return;
    }
    for (int i = start; i <= 9; i++) {
        if (curSum + i > n) break;       // 剪枝：已经超了
        path.push_back(i); curSum += i;
        backtrack(k, n, i + 1);
        path.pop_back();  curSum -= i;
    }
}
```

> 口诀：**组合求和 III，路径够长判和；curSum 同步加减，超了就剪枝**

---

#### 39. 组合总和 ⭐⭐⭐

[LeetCode 39](https://leetcode.cn/problems/combination-sum/)

- **核心**：与 216 几乎相同，但**元素可重复选** → 递归传 `i` 而不是 `i + 1`
- **易错**：传了 `i + 1` 就不能重复选；必须先 `sort` 才能剪枝

```cpp
void backtrack(vector<int>& cand, int remain, int start) {
    if (remain == 0) { res.push_back(path); return; }
    for (int i = start; i < cand.size() && cand[i] <= remain; i++) {
        path.push_back(cand[i]);
        backtrack(cand, remain - cand[i], i);   // 传 i，可重复
        path.pop_back();
    }
}
// 调用前：sort(candidates.begin(), candidates.end());
```

> 口诀：**组合总和可重复，递归传 i 不传 i+1；排序剪枝不能少**

---

#### 40. 组合总和 II ⭐⭐⭐

[LeetCode 40](https://leetcode.cn/problems/combination-sum-ii/)

- **核心**：在 39 基础上，数组有重复元素 → 增加**树层去重**
- **易错**：去重条件 `i > startIndex` 不能写成 `i > 0`（会误杀树枝上的重复）

```cpp
void backtrack(vector<int>& cand, int remain, int start) {
    if (remain == 0) { res.push_back(path); return; }
    for (int i = start; i < cand.size() && cand[i] <= remain; i++) {
        if (i > start && cand[i] == cand[i - 1]) continue;  // 🔑 树层去重

        path.push_back(cand[i]);
        backtrack(cand, remain - cand[i], i + 1);            // i+1，不重复用
        path.pop_back();
    }
}
// 调用前：sort(candidates.begin(), candidates.end());  ← 必须！
```

> 口诀：**组合总和 II，排序去重不能少；树层重复要跳过，树枝重复可以留**

---

### 第二阶段：子集问题（2 题）

---

#### 78. 子集 ⭐⭐

[LeetCode 78](https://leetcode.cn/problems/subsets/)

- **核心**：**所有节点都是结果**——`res.push_back(path)` 放第一行，不用等终止条件
- **易错**：放到终止条件里会漏掉空集和中间路径

```cpp
void backtrack(vector<int>& nums, int start) {
    res.push_back(path);                         // 🔑 进来就加
    for (int i = start; i < nums.size(); i++) {
        path.push_back(nums[i]);
        backtrack(nums, i + 1);
        path.pop_back();
    }
}
```

> 口诀：**子集最简单，进来就加不用管；所有节点都是果，选完回溯继续转**

---

#### 90. 子集 II ⭐⭐

[LeetCode 90](https://leetcode.cn/problems/subsets-ii/)

- **核心**：78 + 树层去重 = 和 40 题完全相同的 `i > start && nums[i] == nums[i-1]`
- **易错**：去重条件别写成 `i > 0`；必须先 `sort`

```cpp
void backtrack(vector<int>& nums, int start) {
    res.push_back(path);
    for (int i = start; i < nums.size(); i++) {
        if (i > start && nums[i] == nums[i - 1]) continue;  // 树层去重
        path.push_back(nums[i]);
        backtrack(nums, i + 1);
        path.pop_back();
    }
}
```

> 口诀：**子集 II 有重复，排序去重不能漏；树层重复要跳过，树枝重复可以留**

---

### 第三阶段：排列问题（2 题）

---

#### 46. 全排列 ⭐⭐⭐

[LeetCode 46](https://leetcode.cn/problems/permutations/)

- **核心**：排列有序 → 不用 `startIndex`，用 `used` 数组标记已选元素
- **易错**：用了 `startIndex` 会漏掉大量排列；`used[i]` 的 push 和 pop 都要对称

```cpp
vector<bool> used;
void backtrack(vector<int>& nums) {
    if (path.size() == nums.size()) { res.push_back(path); return; }
    for (int i = 0; i < nums.size(); i++) {      // 从 0 开始！
        if (used[i]) continue;
        used[i] = true;  path.push_back(nums[i]);
        backtrack(nums);
        used[i] = false; path.pop_back();
    }
}
```

> 口诀：**排列不用 start，used 数组来把关；用过跳过用完还**

---

#### 47. 全排列 II ⭐⭐⭐

[LeetCode 47](https://leetcode.cn/problems/permutations-ii/)

- **核心**：46 + 排列独有的去重——`nums[i]==nums[i-1] && !used[i-1]`
- **易错**：`!used[i-1]` 的含义是"前一个相同元素不在当前路径中"= 同层重复

```cpp
vector<bool> used;
void backtrack(vector<int>& nums) {
    if (path.size() == nums.size()) { res.push_back(path); return; }
    for (int i = 0; i < nums.size(); i++) {
        // 🔑 排列去重：同层重复且前一个未使用 → 跳过
        if (i > 0 && nums[i] == nums[i - 1] && !used[i - 1]) continue;
        if (used[i]) continue;

        used[i] = true;  path.push_back(nums[i]);
        backtrack(nums);
        used[i] = false; path.pop_back();
    }
}
// 调用前：sort(nums.begin(), nums.end());  ← 必须！
```

> 口诀：**排列去重，排序不能少；!used[i-1] 同层重复跳**

---

### 第四阶段：分割问题（2 题）

---

#### 131. 分割回文串 ⭐⭐

[LeetCode 131](https://leetcode.cn/problems/palindrome-partitioning/)

- **核心**：用 `startIndex` 控制切点，切出的子串判回文，合法就继续
- **易错**：不是排列，不要用 `used`；终止条件是切到末尾不是整个串是回文

```cpp
bool isPal(const string& s, int l, int r) {
    while (l < r) if (s[l++] != s[r--]) return false;
    return true;
}

void backtrack(const string& s, int start) {
    if (start == s.size()) { res.push_back(path); return; }

    for (int i = start; i < s.size(); i++) {
        if (!isPal(s, start, i)) continue;
        path.push_back(s.substr(start, i - start + 1));
        backtrack(s, i + 1);
        path.pop_back();
    }
}
```

> 口诀：**分割回文串，start 来控切；子串判回文，是就继续切**

---

#### 93. 复原 IP 地址 ⭐⭐

[LeetCode 93](https://leetcode.cn/problems/restore-ip-addresses/)

- **核心**：131 的变种——回文判断换成 IP 段合法性，终止条件 4 段且切完
- **易错**：前导零 `"0x"` 非法；段长度 1~3；数值 > 255 非法

```cpp
bool valid(const string& s, int l, int r) {
    if (l > r || r - l + 1 > 3) return false;
    if (s[l] == '0' && l != r)   return false;   // 前导零
    int num = 0;
    for (int i = l; i <= r; i++) {
        num = num * 10 + (s[i] - '0');
        if (num > 255) return false;
    }
    return true;
}

void backtrack(const string& s, int start) {
    if (path.size() == 4) {
        if (start == s.size()) { /* 拼接成 "a.b.c.d" 加入 res */ }
        return;
    }
    for (int i = start; i < s.size() && i < start + 3; i++) {
        if (!valid(s, start, i)) continue;
        path.push_back(s.substr(start, i - start + 1));
        backtrack(s, i + 1);
        path.pop_back();
    }
}
```

> 口诀：**复原 IP，四段来切；段段合法才下探，零头超 255 都不行**

---

### 第五阶段：棋盘问题（1 题）

---

#### 51. N 皇后 ⭐⭐

[LeetCode 51](https://leetcode.cn/problems/n-queens/)

- **核心**：按行递归，每行选一个列放 Q；检查列 + 左上对角线 + 右上对角线
- **易错**：对角线只需查当前行以上；列也只查当前行以上

```cpp
bool valid(int row, int col, vector<string>& board, int n) {
    for (int i = 0; i < row; i++)                     // 列
        if (board[i][col] == 'Q') return false;
    for (int i = row-1, j = col-1; i >= 0 && j >= 0; i--, j--)  // 左上
        if (board[i][j] == 'Q') return false;
    for (int i = row-1, j = col+1; i >= 0 && j < n;  i--, j++)  // 右上
        if (board[i][j] == 'Q') return false;
    return true;
}

void backtrack(int n, int row, vector<string>& board) {
    if (row == n) { res.push_back(board); return; }
    for (int col = 0; col < n; col++) {
        if (!valid(row, col, board, n)) continue;
        board[row][col] = 'Q';
        backtrack(n, row + 1, board);
        board[row][col] = '.';
    }
}
```

> 口诀：**N 皇后按行放，列和斜线不能撞；放完递归下一行，不行就撤换地方**

---

## 四、去重逻辑终极对比 🔑

> 核心原则：**树层去重，树枝不去重**

| 问题类型 | 去重代码 | 原理 |
|----------|---------|------|
| **组合/子集** | `if (i > start && nums[i] == nums[i-1]) continue;` | `i > start` 保证只在同层跳过 |
| **排列** | `if (i > 0 && nums[i] == nums[i-1] && !used[i-1]) continue;` | `!used[i-1]` 排除树枝上的重复 |

> **记忆技巧**：组合/子集用 `startIndex` 天然区分，排列用 `!used[i-1]` 人工区分

---

## 五、控制方式速查

| 问题 | 是否有序 | 控制变量 | 递归传参 | 收集时机 |
|------|---------|---------|---------|---------|
| 组合 | 无序 | `startIndex` | `i + 1` | 叶子（path.size() == k） |
| 子集 | 无序 | `startIndex` | `i + 1` | **所有节点** |
| 排列 | 有序 | `used[]` | 无 | 叶子（path.size() == n） |
| 分割 | 无序 | `startIndex` | `i + 1` | 叶子（切到末尾） |

---

## 六、机试频率排行

| 题号 | 题目 | 频率 | 难度 |
|------|------|------|------|
| 39 | 组合总和 | ⭐⭐⭐⭐⭐ | 中等 |
| 46 | 全排列 | ⭐⭐⭐⭐⭐ | 中等 |
| 78 | 子集 | ⭐⭐⭐⭐⭐ | 中等 |
| 40 | 组合总和 II | ⭐⭐⭐⭐ | 中等 |
| 47 | 全排列 II | ⭐⭐⭐⭐ | 中等 |
| 90 | 子集 II | ⭐⭐⭐⭐ | 中等 |
| 131 | 分割回文串 | ⭐⭐⭐⭐ | 中等 |
| 77 | 组合 | ⭐⭐⭐ | 中等 |
| 93 | 复原 IP 地址 | ⭐⭐⭐ | 中等 |
| 216 | 组合总和 III | ⭐⭐⭐ | 中等 |
| 51 | N 皇后 | ⭐⭐ | 困难 |

---

## 七、考前一分钟速记

```
万能模板     →  做选择 → 递归 → 撤销
组合子集     →  startIndex，无序控范围
排列         →  used 数组，有序控重复
子集特殊     →  进来就加，所有节点都是结果
去重铁律     →  树层去重，树枝不去重
回溯对称     →  做了什么选择，就撤销什么选择
```

---

> **状态**：回溯算法板块 ✅ 锁死 | **下一板块**：动态规划 / 贪心
