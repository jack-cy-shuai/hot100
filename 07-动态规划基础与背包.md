# 动态规划基础 + 背包 · 机试核心速记手册

> 今日覆盖：17 题 | 基础 DP（8 题）+ 01 背包（5 题）+ 完全背包（4 题）

---

## 一、DP 解题五部曲（所有题通用）

```
① 确定 dp[i] / dp[i][j] 含义        ← 最关键
② 推导递推公式（状态转移）
③ 初始化边界条件
④ 确定遍历顺序（保证依赖已经算完）
⑤ 用小例子手动推导验证
```

---

## 二、基础 DP（8 题）

---

### 509. 斐波那契数 ⭐

[LeetCode 509](https://leetcode.cn/problems/fibonacci-number/)

- **递推**：`dp[i] = dp[i-1] + dp[i-2]`
- **最优**：三变量滚动，O(n) 时间 O(1) 空间

```cpp
int fib(int n) {
    if (n <= 1) return n;
    int a = 0, b = 1, c;
    for (int i = 2; i <= n; i++) {
        c = a + b;
        a = b; b = c;
    }
    return b;
}
```

---

### 70. 爬楼梯 ⭐⭐⭐

[LeetCode 70](https://leetcode.cn/problems/climbing-stairs/)

- **递推**：`dp[i] = dp[i-1] + dp[i-2]`（每次 1 或 2 步）
- **初始**：`dp[1] = 1, dp[2] = 2`
- 本质就是斐波那契，代码和 509 一样

```cpp
int climbStairs(int n) {
    if (n <= 2) return n;
    int a = 1, b = 2, c;
    for (int i = 3; i <= n; i++) {
        c = a + b;
        a = b; b = c;
    }
    return b;
}
```

---

### 746. 使用最小花费爬楼梯 ⭐

[LeetCode 746](https://leetcode.cn/problems/min-cost-climbing-stairs/)

- **递推**：`dp[i] = min(dp[i-1], dp[i-2]) + cost[i]`
- **结果**：`min(dp[n-1], dp[n-2])`（顶部在最后一阶之后）

```cpp
int minCostClimbingStairs(vector<int>& cost) {
    int n = cost.size();
    int a = cost[0], b = cost[1];           // 从第 0 或第 1 阶开始
    for (int i = 2; i < n; i++) {
        int c = min(a, b) + cost[i];
        a = b; b = c;
    }
    return min(a, b);                       // 最后一步可以跨过顶层
}
```

---

### 62. 不同路径 ⭐⭐⭐

[LeetCode 62](https://leetcode.cn/problems/unique-paths/)

- **递推**：`dp[i][j] = dp[i-1][j] + dp[i][j-1]`
- **初始化**：首行首列全是 1

```cpp
int uniquePaths(int m, int n) {
    vector<vector<int>> dp(m, vector<int>(n, 1));
    for (int i = 1; i < m; i++)
        for (int j = 1; j < n; j++)
            dp[i][j] = dp[i-1][j] + dp[i][j-1];
    return dp[m-1][n-1];
}

// 空间优化：一维数组 O(n)
int uniquePaths(int m, int n) {
    vector<int> dp(n, 1);
    for (int i = 1; i < m; i++)
        for (int j = 1; j < n; j++)
            dp[j] += dp[j-1];              // dp[j] 即上格 + 左格
    return dp[n-1];
}
```

---

### 63. 不同路径 II ⭐⭐

[LeetCode 63](https://leetcode.cn/problems/unique-paths-ii/)

- **递推**：`dp[i][j] = (grid[i][j]==1) ? 0 : dp[i-1][j] + dp[i][j-1]`
- **易错**：起点就是障碍直接 return 0；首行首列遇障碍后全 0

```cpp
int uniquePathsWithObstacles(vector<vector<int>>& g) {
    int m = g.size(), n = g[0].size();
    if (g[0][0] == 1) return 0;            // 起点堵死

    vector<vector<int>> dp(m, vector<int>(n, 0));
    dp[0][0] = 1;
    for (int i = 1; i < m && g[i][0] == 0; i++) dp[i][0] = 1;  // 首列遇障后断
    for (int j = 1; j < n && g[0][j] == 0; j++) dp[0][j] = 1;

    for (int i = 1; i < m; i++)
        for (int j = 1; j < n; j++)
            if (g[i][j] == 0)
                dp[i][j] = dp[i-1][j] + dp[i][j-1];
    return dp[m-1][n-1];
}
```

---

### 64. 最小路径和 ⭐⭐

[LeetCode 64](https://leetcode.cn/problems/minimum-path-sum/)

- **递推**：`dp[i][j] = min(dp[i-1][j], dp[i][j-1]) + grid[i][j]`
- **初始化**：首行首列累加

```cpp
int minPathSum(vector<vector<int>>& grid) {
    int m = grid.size(), n = grid[0].size();
    vector<vector<int>> dp(m, vector<int>(n, 0));
    dp[0][0] = grid[0][0];
    for (int i = 1; i < m; i++) dp[i][0] = dp[i-1][0] + grid[i][0];  // 首列累加
    for (int j = 1; j < n; j++) dp[0][j] = dp[0][j-1] + grid[0][j];  // 首行累加
    for (int i = 1; i < m; i++)
        for (int j = 1; j < n; j++)
            dp[i][j] = min(dp[i-1][j], dp[i][j-1]) + grid[i][j];
    return dp[m-1][n-1];
}
```

---

### 343. 整数拆分 ⭐⭐

[LeetCode 343](https://leetcode.cn/problems/integer-break/)

- **递推**：`dp[i] = max(dp[i], max(j*(i-j), j*dp[i-j]))`
- **含义**：拆出 j，`i-j` 可选择继续拆（`dp[i-j]`）或不拆（`i-j`）
- **初始**：`dp[2] = 1`

```cpp
int integerBreak(int n) {
    vector<int> dp(n + 1, 0);
    dp[2] = 1;
    for (int i = 3; i <= n; i++)
        for (int j = 1; j < i; j++)
            dp[i] = max(dp[i], max(j * (i - j), j * dp[i - j]));
    return dp[n];
}
```

> 贪心技巧：尽可能多拆成 3，剩下拆 2（`n==4` 时拆成 `2×2`）

---

### 96. 不同的二叉搜索树 ⭐⭐

[LeetCode 96](https://leetcode.cn/problems/unique-binary-search-trees/)

- **递推**：`dp[i] += dp[j-1] * dp[i-j]`（`j` 为根节点编号）
- **含义**：左子树 `j-1` 个节点，右子树 `i-j` 个节点，形态数相乘
- **初始**：`dp[0] = 1, dp[1] = 1`

```cpp
int numTrees(int n) {
    vector<int> dp(n + 1, 0);
    dp[0] = dp[1] = 1;
    for (int i = 2; i <= n; i++)
        for (int j = 1; j <= i; j++)
            dp[i] += dp[j-1] * dp[i-j];
    return dp[n];
}
```

> 本质是卡特兰数：`C_n = C(2n,n) / (n+1)`

---

### 基础 DP 对比总结

| 题目 | 递推公式 | 初始化要点 | 滚动优化 |
|------|---------|-----------|---------|
| 509 斐波那契 | `dp[i] = dp[i-1] + dp[i-2]` | `dp[0]=0, dp[1]=1` | 三变量 ✅ |
| 70 爬楼梯 | 同上 | `dp[1]=1, dp[2]=2` | 三变量 ✅ |
| 746 最小花费 | `dp[i] = min(dp[i-1],dp[i-2]) + cost[i]` | `dp[0]=cost[0], dp[1]=cost[1]` | 三变量 ✅ |
| 62 不同路径 | `dp[i][j] = dp[i-1][j] + dp[i][j-1]` | 首行首列 = 1 | 一维 ✅ |
| 63 路径带障碍 | 同上但有障碍置 0 | 遇障后全 0 | 一维 ✅ |
| 64 最小路径和 | `dp[i][j] = min(上,左) + grid[i][j]` | 首行首列累加 | 一维 ✅ |
| 343 整数拆分 | `dp[i] = max(j*(i-j), j*dp[i-j])` | `dp[2]=1` | ❌ |
| 96 不同 BST | `dp[i] += dp[j-1] * dp[i-j]` | `dp[0]=dp[1]=1` | ❌ |

---

## 三、01 背包（5 题）

> **核心**：每件物品只能用一次 → 容量 **从后往前** 遍历

---

### 01 背包模板

```cpp
// 一维 DP（机试首选）
int knapsack(int W, vector<int>& weight, vector<int>& value) {
    vector<int> dp(W + 1, 0);
    for (int i = 0; i < weight.size(); i++)           // 先物品
        for (int j = W; j >= weight[i]; j--)          // 🔑 从后往前
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]);
    return dp[W];
}
```

### 01 背包应用题

---

#### 416. 分割等和子集 ⭐⭐⭐

[LeetCode 416](https://leetcode.cn/problems/partition-equal-subset-sum/)

- **转化**：能否装满容量 `sum/2` 的背包，物品重量 = 价值 = `nums[i]`
- **关键**：`sum` 为奇数直接 false

```cpp
bool canPartition(vector<int>& nums) {
    int sum = accumulate(nums.begin(), nums.end(), 0);
    if (sum % 2 != 0) return false;                  // 奇数必不行

    int target = sum / 2;
    vector<int> dp(target + 1, 0);
    for (int num : nums)
        for (int j = target; j >= num; j--)
            dp[j] = max(dp[j], dp[j - num] + num);
    return dp[target] == target;                     // 装满
}
```

---

#### 1049. 最后一块石头的重量 II ⭐⭐

[LeetCode 1049](https://leetcode.cn/problems/last-stone-weight-ii/)

- **转化**：分成重量最接近的两堆 → 背包容量 `sum/2`，求最大可装重量
- **结果**：`sum - 2 × dp[sum/2]`

```cpp
int lastStoneWeightII(vector<int>& stones) {
    int sum = accumulate(stones.begin(), stones.end(), 0);
    int target = sum / 2;
    vector<int> dp(target + 1, 0);
    for (int s : stones)
        for (int j = target; j >= s; j--)
            dp[j] = max(dp[j], dp[j - s] + s);
    return sum - 2 * dp[target];
}
```

> 和 416 几乎一样，只是返回值不同：`sum - 2*dp[target]`

---

#### 494. 目标和 ⭐⭐⭐

[LeetCode 494](https://leetcode.cn/problems/target-sum/)

- **转化**：`x = (sum + target) / 2`，求凑成和 `x` 的方案数
- **递推**：`dp[j] += dp[j - num]`（求方案数，不是 max！）
- **初始**：`dp[0] = 1`
- **易错**：`sum+target` 为奇数或 `abs(target) > sum` 无解

```cpp
int findTargetSumWays(vector<int>& nums, int target) {
    int sum = accumulate(nums.begin(), nums.end(), 0);
    if (abs(target) > sum || (sum + target) % 2 != 0) return 0;

    int x = (sum + target) / 2;
    vector<int> dp(x + 1, 0);
    dp[0] = 1;                                       // 🔑 方案数 dp[0]=1
    for (int num : nums)
        for (int j = x; j >= num; j--)
            dp[j] += dp[j - num];                     // 🔑 += 不是 max
    return dp[x];
}
```

---

#### 474. 一和零 ⭐⭐

[LeetCode 474](https://leetcode.cn/problems/ones-and-zeroes/)

- **转化**：二维费用 01 背包——容量 = `(m, n)`，每个字符串有 `(zeros, ones)`
- **递推**：`dp[i][j] = max(dp[i][j], dp[i-zero][j-one] + 1)`
- **遍历**：两个维度都从后往前

```cpp
int findMaxForm(vector<string>& strs, int m, int n) {
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    for (string& s : strs) {
        int zeros = count(s.begin(), s.end(), '0');
        int ones  = s.size() - zeros;
        for (int i = m; i >= zeros; i--)             // 两个维度都从后往前
            for (int j = n; j >= ones; j--)
                dp[i][j] = max(dp[i][j], dp[i - zeros][j - ones] + 1);
    }
    return dp[m][n];
}
```

---

### 01 背包题型速查

| 题目 | dp 含义 | 递推公式 | 初始化 | 结果 |
|------|---------|---------|--------|------|
| 纯 01 背包 | 容量 j 的最大价值 | `max(dp[j], dp[j-w]+v)` | `dp[0]=0` | `dp[W]` |
| 416 分割等和 | 容量 j 的最大价值 | `max(dp[j], dp[j-num]+num)` | `dp[0]=0` | `dp[target]==target` |
| 1049 石头 | 容量 j 的最大重量 | `max(dp[j], dp[j-s]+s)` | `dp[0]=0` | `sum-2*dp[sum/2]` |
| 494 目标和 | **凑成 j 的方案数** | `dp[j] += dp[j-num]` | `dp[0]=1` | `dp[x]` |
| 474 一和零 | i 个 0 j 个 1 的最长子集 | `max(dp[i][j], dp[i-z][j-o]+1)` | `dp[0][0]=0` | `dp[m][n]` |

---

## 四、完全背包（4 题）

> **核心**：每件物品无限次使用 → 容量 **从前往后** 遍历

---

### 完全背包模板

```cpp
int completeKnapsack(int W, vector<int>& weight, vector<int>& value) {
    vector<int> dp(W + 1, 0);
    for (int i = 0; i < weight.size(); i++)           // 先物品
        for (int j = weight[i]; j <= W; j++)          // 🔑 从前往后
            dp[j] = max(dp[j], dp[j - weight[i]] + value[i]);
    return dp[W];
}
```

### 01 vs 完全背�包唯一区别

| | 01 背包 | 完全背包 |
|--|--------|---------|
| 物品 | 只能用一次 | 无限次 |
| 容量遍历 | **从后往前** | **从前往后** |
| 二维递推 | 用上一行 `dp[i-1][j-w]` | 用当前行 `dp[i][j-w]` |

---

### 完全背包应用题

---

#### 518. 零钱兑换 II ⭐⭐⭐

[LeetCode 518](https://leetcode.cn/problems/coin-change-ii/)

- **核心**：求凑成 amount 的**组合数**
- **递推**：`dp[j] += dp[j - coin]`
- **初始**：`dp[0] = 1`
- **遍历顺序 = 组合 vs 排列的关键**：
  - 先物品后背包 → **组合数**（顺序无关）
  - 先背包后物品 → 排列数（顺序有关）

```cpp
int change(int amount, vector<int>& coins) {
    vector<int> dp(amount + 1, 0);
    dp[0] = 1;
    for (int coin : coins)                           // 先物品
        for (int j = coin; j <= amount; j++)         // 从前往后
            dp[j] += dp[j - coin];                   // 组合数
    return dp[amount];
}
```

---

#### 322. 零钱兑换 ⭐⭐⭐

[LeetCode 322](https://leetcode.cn/problems/coin-change/)

- **核心**：凑成 amount 的**最少硬币数**（求最小值）
- **递推**：`dp[j] = min(dp[j], dp[j - coin] + 1)`
- **初始**：`dp[0] = 0`，其余 `INT_MAX / 2`（防 +1 溢出）
- **注意**：求最小值时遍历顺序不影响结果

```cpp
int coinChange(vector<int>& coins, int amount) {
    vector<int> dp(amount + 1, INT_MAX / 2);         // 🔑 防溢出
    dp[0] = 0;
    for (int coin : coins)
        for (int j = coin; j <= amount; j++)
            dp[j] = min(dp[j], dp[j - coin] + 1);
    return dp[amount] == INT_MAX / 2 ? -1 : dp[amount];
}
```

---

#### 279. 完全平方数 ⭐⭐

[LeetCode 279](https://leetcode.cn/problems/perfect-squares/)

- **转化**：和 322 完全一样——硬币就是完全平方数 `1, 4, 9, 16...`

```cpp
int numSquares(int n) {
    vector<int> dp(n + 1, INT_MAX / 2);
    dp[0] = 0;
    for (int i = 1; i * i <= n; i++)                 // 物品 = 完全平方数
        for (int j = i * i; j <= n; j++)
            dp[j] = min(dp[j], dp[j - i * i] + 1);
    return dp[n];
}
```

---

### 完全背包题型速查

| 题目 | dp 含义 | 递推公式 | 初始化 | 遍历顺序 |
|------|---------|---------|--------|----------|
| 纯完全背包 | 最大价值 | `max(dp[j], dp[j-w]+v)` | `dp[0]=0` | 不限 |
| 518 零钱 II | **组合数** | `dp[j] += dp[j-coin]` | `dp[0]=1` | **必须先物品后背包** |
| 322 零钱 | **最少硬币数** | `min(dp[j], dp[j-coin]+1)` | `dp[0]=0`, 其余 `∞` | 不限 |
| 279 平方数 | **最少平方数** | `min(dp[j], dp[j-s²]+1)` | `dp[0]=0`, 其余 `∞` | 不限 |

---

## 五、操作类型 → 递推公式 速查

| 操作 | 递推公式 | 初始化 | 典型题目 |
|------|---------|--------|----------|
| **求最大值** | `dp[j] = max(dp[j], dp[j-w] + v)` | `dp[0]=0` | 纯背包、416、1049、474 |
| **求方案数** | `dp[j] += dp[j - w]` | `dp[0]=1` | 494、518 |
| **求最小值** | `dp[j] = min(dp[j], dp[j-w] + 1)` | `dp[0]=0`, 其余 `INT_MAX/2` | 322、279 |

---

## 六、背包题转化思路

```
拿到应用题 → 找三个要素：
  ① 物品：数组元素 / 字符串 / 硬币
  ② 重量：数值大小 / 0的个数 / 面额
  ③ 容量：target / sum/2 / amount

  → 确定是 01 还是完全（物品用一次还是无限次）
  → 确定是求 max / 方案数 / min
  → 套对应模板
```

---

## 七、高频致命坑点

| # | 坑点 | 说明 |
|---|------|------|
| 1 | **01/完全遍历方向反了** | 01 从后往前，完全从前往后 |
| 2 | **方案数 dp[0] = 0** | 求方案数 `dp[0]` 必须初始化为 1 |
| 3 | **INT_MAX + 1 溢出** | 求最小值用 `INT_MAX / 2` 当无穷大 |
| 4 | **518 遍历顺序错** | 先背包后物品会变成排列数 |
| 5 | **494 忘了判奇数** | `sum+target` 奇数直接 return 0 |
| 6 | **416 忘了判奇数** | `sum` 奇数直接 return false |

---

## 八、考前一分钟速记

```
DP 五步走     →  含义 → 递推 → 初始化 → 遍历 → 验证
01 背包       →  物品一次，容量从后往前
完全背包      →  物品无限，容量从前往后
求最大值      →  max，dp[0] = 0
求方案数      →  +=，dp[0] = 1
求最小值      →  min，dp[0] = 0，其余 INT_MAX/2
组合 vs 排列  →  先物品 = 组合，先背包 = 排列
```

---

> **状态**：DP 基础 + 背包板块 ✅ 锁死 | **下一板块**：子序列 DP / 打家劫舍
