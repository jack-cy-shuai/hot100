# 二刷复盘 · 前 10 题（Day1-5）

> 状态：6 题完美 ✅ | 4 题细节扣分 ⚠️ | 0 题思路卡壳

---

## 一、完成清单

| 天 | 题号 | 题目 | 模块 | 结果 |
|----|------|------|------|------|
| 1 | 283 | 移动零 | 数组双指针 | ✅ |
| 1 | 3 | 无重复字符的最长子串 | 滑动窗口 | ✅ |
| 2 | 209 | 长度最小的子数组 | 滑动窗口 | ⚠️ 题意 |
| 2 | 438 | 找到字符串中所有字母异位词 | 固定窗口 | ✅ |
| 3 | 27 | 移除元素 | 数组双指针 | ⚠️ 冗余 |
| 3 | 26 | 删除有序数组中的重复项 | 数组双指针 | ⚠️ 边界 |
| 4 | 206 | 反转链表 | 链表 | ✅ |
| 4 | 19 | 删除链表的倒数第 N 个节点 | 链表 | ⚠️ dummy |
| 5 | 141 | 环形链表 | 链表快慢指针 | ⚠️ 判空 |
| 5 | 142 | 环形链表 II | 链表快慢指针 | ⚠️ 变量 |

---

## 二、做得好的地方

1. **题型识别快**：看到题就知道用什么模板（快慢指针/滑动窗口/虚拟头/快慢判环），思路不卡
2. **深挖原理**：141/142 主动要求证明快慢指针相遇定理和环入口公式
3. **代码规范**：变量命名 `slow/fast/pre/dummy` 通用，复杂逻辑拆分辅助函数
4. **纠错效率高**：指出问题后能立刻理解并修正

---

## 三、高频共性问题（按频率排序）

### 🔴 第一：边界条件遗漏

| 问题 | 出现题目 | 正确做法 |
|------|---------|---------|
| 空数组未判空 | 26 | 函数开头 `if (nums.empty()) return 0` |
| `&&` 和 `||` 混用 | 141 | 链表判空用 `||`：`!fast || !fast->next` |
| 题意 `≥` 写成 `>` | 209 | 圈出题眼，区分 `≥` 和 `>` |

### 🔴 第二：链表空指针安全

| 问题 | 出现题目 | 正确做法 |
|------|---------|---------|
| 直接写 `fast->next->next` | 19/141 | 必须先判 `fast && fast->next` |
| 忘记 `dummy->next = head` | 19 | 创建 dummy 后第一行挂载 |
| 删除后返回原 `head` | 19 | 统一返回 `dummy->next` |

### 🟡 第三：变量/循环细节

| 问题 | 出现题目 | 正确做法 |
|------|---------|---------|
| 同作用域重复定义变量 | 27 | 删掉内层重复声明 |
| 循环条件误用不变变量 | 142 | 检查条件变量是否会随循环改变 |

---

## 四、链表铁律（最容易失分，必须刻进肌肉记忆）

```
1. x->next 出现前，先保证 x != nullptr
2. 快慢指针循环统一写：
   while (fast != nullptr && fast->next != nullptr)
3. 虚拟头结点标准三行：
   dummy->next = head;     // ① 挂载
   // ... 操作 ...
   return dummy->next;     // ② 返回，不是 head
```

---

## 五、做题前置检查清单（每次写代码前过一遍）

```
□ 圈题眼：原地？有序？≥还是＞？返回什么？
□ 极端用例：空输入？单元素？全相同？
□ 选模板：双指针？滑动窗口？链表 dummy？
□ 写代码后自测：空/单/极端 三个用例
```

---

## 六、改进措施

| 习惯 | 具体操作 |
|------|---------|
| 写前 30 秒 | 圈题眼 + 想三个极端用例 |
| 链表题 | 强制先写 `dummy->next = head` 和判空条件 |
| 做错后 | 一句话记错因在题旁，新题开始前翻一眼 |
| 变量规范 | 同作用域不重复定义；统一用 `nullptr` 不用 `NULL` |

---

> **结论**：思路全部到位，问题全在细节。养成边界预判 + 链表指针保护习惯，正确率会大幅提升。

---

> 下一组：Day6 - 栈 + 单调队列（20 有效括号、239 滑动窗口最大值）
