# DP 进阶 · 子序列 + 打家劫舍 + 综合拓展

> 今日覆盖：13 题 | LIS / LCS / 回文 / 树形DP / 状压DP | DP 终极收官

衔接 [07-动态规划基础与背包](07-动态规划基础与背包.md)

---

## 一、本板块三大子模块

| 子模块 | 题数 | 核心模型 | 代表题 |
|--------|------|---------|--------|
| **子序列 DP** | 6 | 单序列 / 双序列 / 区间 DP | 300 / 1143 / 516 / 647 |
| **打家劫舍** | 3 | 选/不选 → 线性 / 环形 / 树形 | 198 / 213 / 337 |
| **综合拓展** | 4 | 矩阵 / 组合 / 状压 DP | 221 / 132 / 698 / 473 |

---

## 二、子序列 DP（6 题）

> 核心区分：**连续** vs **不连续**，这是子序列题的命门

---

### 674. 最长连续递增子序列 ⭐⭐

[LeetCode 674](https://leetcode.cn/problems/longest-continuous-increasing-subsequence/)

- **核心**：连续 → 只和前一位比，`nums[i] > nums[i-1]` 则 `dp[i] = dp[i-1] + 1`
- **易错**：结果是 `max_element(dp)` 不是 `dp[n-1]`

```cpp
int findLengthOfLCIS(vector<int>& nums) {
    if (nums.empty()) return 0;
    int n = nums.size();
    vector<int> dp(n, 1);
    for (int i = 1; i < n; i++)
        if (nums[i] > nums[i - 1])
            dp[i] = dp[i - 1] + 1;
    return *max_element(dp.begin(), dp.end());
}
```

> 口诀：**连续只看前一位，递增加一，否则归一，最后取最大**

---

### 300. 最长递增子序列（LIS）⭐⭐⭐

[LeetCode 300](https://leetcode.cn/problems/longest-increasing-subsequence/)

- **核心**：不连续 → 遍历前方所有 `j < i`，`nums[j] < nums[i]` 则 `dp[i] = max(dp[i], dp[j] + 1)`
- **易错**：O(n²) 为基础解；实时更新 `res` 避免最后遍历

```cpp
int lengthOfLIS(vector<int>& nums) {
    int n = nums.size(), res = 1;
    vector<int> dp(n, 1);
    for (int i = 1; i < n; i++) {
        for (int j = 0; j < i; j++)
            if (nums[j] < nums[i])
                dp[i] = max(dp[i], dp[j] + 1);
        res = max(res, dp[i]);
    }
    return res;
}
```

> 进阶：贪心 + 二分，O(n log n)

> 口诀：**不连续看前面所有，比当前小就尝试拼，实时更新最大值**

---

### 674 vs 300 对比

| | 674 连续递增 | 300 不连续递增 |
|--|------------|-------------|
| 内层 | 只看 `i-1` | 遍历 `0 ~ i-1` |
| 不等时 | `dp[i] = 1` | 保持原值 |
| 时间 | O(n) | O(n²) |

---

### 1143. 最长公共子序列（LCS）⭐⭐⭐

[LeetCode 1143](https://leetcode.cn/problems/longest-common-subsequence/)

- **核心**：双序列二维 DP 模板——相等取左上+1，不等取左/上较大
- **易错**：`dp[i][j]` 对应原串 `i-1`、`j-1` 位置（错位一位）

```
相等：dp[i][j] = dp[i-1][j-1] + 1
不等：dp[i][j] = max(dp[i-1][j], dp[i][j-1])
```

```cpp
int longestCommonSubsequence(string text1, string text2) {
    int m = text1.size(), n = text2.size();
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    for (int i = 1; i <= m; i++)
        for (int j = 1; j <= n; j++)
            if (text1[i - 1] == text2[j - 1])
                dp[i][j] = dp[i - 1][j - 1] + 1;
            else
                dp[i][j] = max(dp[i - 1][j], dp[i][j - 1]);
    return dp[m][n];
}
```

> 口诀：**两串各一维，相等左上+1，不等取左右大**

---

### 718. 最长重复子数组 ⭐⭐

[LeetCode 718](https://leetcode.cn/problems/maximum-length-of-repeated-subarray/)

- **核心**：连续版本 LCS——不等直接置 0，实时记录 max
- **易错**：连续结构不等就清零；结果不在 `dp[m][n]`

```cpp
int findLength(vector<int>& nums1, vector<int>& nums2) {
    int m = nums1.size(), n = nums2.size(), res = 0;
    vector<vector<int>> dp(m + 1, vector<int>(n + 1, 0));
    for (int i = 1; i <= m; i++)
        for (int j = 1; j <= n; j++)
            if (nums1[i - 1] == nums2[j - 1]) {
                dp[i][j] = dp[i - 1][j - 1] + 1;
                res = max(res, dp[i][j]);          // 🔑 实时记录
            }
    return res;
}
```

### 1143 vs 718 对比

| | 1143 LCS 不连续 | 718 连续 |
|--|----------------|---------|
| 不等时 | `max(左, 上)` | **直接置 0** |
| 结果位置 | `dp[m][n]` | 遍历中 `max` |

> 口诀：**连续不等就清零，不连续取左右大**

---

### 516. 最长回文子序列 ⭐⭐

[LeetCode 516](https://leetcode.cn/problems/longest-palindromic-subsequence/)

- **核心**：区间 DP——**先枚举长度，再枚举起点**，不能正序 `i`
- **易错**：遍历顺序是核心——`for(len=2; len<=n; len++) for(i=0; i+len<=n; i++)`

```
s[i]==s[j] → dp[i][j] = dp[i+1][j-1] + 2
s[i]!=s[j] → dp[i][j] = max(dp[i+1][j], dp[i][j-1])
初始化：dp[i][i] = 1
```

```cpp
int longestPalindromeSubseq(string s) {
    int n = s.size();
    vector<vector<int>> dp(n, vector<int>(n, 0));
    for (int i = 0; i < n; i++) dp[i][i] = 1;
    for (int len = 2; len <= n; len++)           // 🔑 先枚举长度
        for (int i = 0; i + len <= n; i++) {
            int j = i + len - 1;
            if (s[i] == s[j])
                dp[i][j] = dp[i + 1][j - 1] + 2;
            else
                dp[i][j] = max(dp[i + 1][j], dp[i][j - 1]);
        }
    return dp[0][n - 1];
}
```

> 口诀：**区间 DP 先枚举长度，两端等加 2，不等取左右**

---

### 516 vs 647 回文子序列 vs 回文子串

| | 516 最长回文子序列 | 647 回文子串 |
|--|------------------|------------|
| 要求 | **不连续** | **连续** |
| 方法 | 区间 DP | 中心扩展 |
| 最优时间 | O(n²) | O(n²) |
| 最优空间 | O(n²) / O(n) | O(1) |

---

### 647. 回文子串 ⭐⭐

[LeetCode 647](https://leetcode.cn/problems/palindromic-substrings/)

- **核心**：中心扩展法——每个字符当作奇/偶两种中心，向两边扩
- **易错**：两种中心都要处理（`(i,i)` 奇数 + `(i,i+1)` 偶数）

```cpp
int countSubstrings(string s) {
    int n = s.size(), cnt = 0;
    for (int i = 0; i < n; i++) {
        expand(s, i, i,   cnt);    // 奇数长度中心
        expand(s, i, i+1, cnt);    // 偶数长度中心
    }
    return cnt;
}

void expand(string& s, int l, int r, int& cnt) {
    while (l >= 0 && r < s.size() && s[l] == s[r]) {
        cnt++; l--; r++;
    }
}
```

> 口诀：**每点做两种中心，向两边扩展；相等就计数，不等立刻停**

---

### 子序列六题总结

| 题号 | 连续？ | 序列数 | DP 类型 | 核心状态 |
|------|--------|--------|---------|---------|
| 674 | ✅ 连续 | 单串 | 一维 | `dp[i]=dp[i-1]+1` 或 `1` |
| 300 | ❌ 非连续 | 单串 | 一维 | `max(dp[i], dp[j]+1)` |
| 1143 | ❌ 非连续 | 双串 | 二维 | 相等+1，不等取左右 |
| 718 | ✅ 连续 | 双串 | 二维 | 相等+1，不等置0 |
| 516 | ❌ 非连续 | 单串回文 | 区间 DP | 先枚举长度 |
| 647 | ✅ 连续 | 单串回文 | 中心扩展 | 奇数+偶数中心 |

---

## 三、打家劫舍系列（3 题）

> 统一规则：不能选相邻元素 → `dp[i] = max(dp[i-1], dp[i-2] + nums[i])`

---

### 198. 打家劫舍 ⭐⭐⭐

[LeetCode 198](https://leetcode.cn/problems/house-robber/)

```cpp
int rob(vector<int>& nums) {
    int n = nums.size();
    if (n == 1) return nums[0];
    if (n == 2) return max(nums[0], nums[1]);
    vector<int> dp(n);
    dp[0] = nums[0]; dp[1] = max(nums[0], nums[1]);
    for (int i = 2; i < n; i++)
        dp[i] = max(dp[i - 1], dp[i - 2] + nums[i]);
    return dp[n - 1];
}
```

> 口诀：**偷或不偷二选一，偷则加隔前，不偷则承前**

---

### 213. 打家劫舍 II ⭐⭐

[LeetCode 213](https://leetcode.cn/problems/house-robber-ii/)

- **核心**：环形 → 拆成两个线性：`[0, n-2]`（去尾）和 `[1, n-1]`（去头），取 max
- **优化**：滚动变量替代 dp 数组，O(1) 空间

```cpp
int rob(vector<int>& nums) {
    int n = nums.size();
    if (n == 1) return nums[0];
    return max(robRange(nums, 0, n - 2), robRange(nums, 1, n - 1));
}

int robRange(vector<int>& nums, int l, int r) {
    if (l == r) return nums[l];
    int prev2 = nums[l], prev1 = max(nums[l], nums[l + 1]);
    for (int i = l + 2; i <= r; i++) {
        int cur = max(prev1, prev2 + nums[i]);
        prev2 = prev1; prev1 = cur;
    }
    return prev1;
}
```

> 口诀：**环形拆两段，去头去尾各算一遍，取最大**

---

### 337. 打家劫舍 III ⭐⭐

[LeetCode 337](https://leetcode.cn/problems/house-robber-iii/)

- **核心**：树形 DP——后序遍历，每节点返回 `{偷, 不偷}` 两个状态
- **易错**：必须后序（先算子树再算根）

```
偷当前 = node.val + 左.不偷 + 右.不偷
不偷当前 = max(左.偷, 左.不偷) + max(右.偷, 右.不偷)
```

```cpp
pair<int, int> dfs(TreeNode* node) {
    if (!node) return {0, 0};
    auto [lRob, lNot] = dfs(node->left);
    auto [rRob, rNot] = dfs(node->right);
    int rob    = node->val + lNot + rNot;                       // 偷当前
    int notRob = max(lRob, lNot) + max(rRob, rNot);            // 不偷当前
    return {rob, notRob};
}

int rob(TreeNode* root) {
    auto [r, n] = dfs(root);
    return max(r, n);
}
```

> 口诀：**树形 DP 后序走，每节点两个状态；偷则加子不偷，不偷取子最大**

---

### 打家劫舍三部曲对比

| | 198 线性 | 213 环形 | 337 树形 |
|--|---------|---------|---------|
| 结构 | 数组 | 数组首尾相连 | 二叉树 |
| 核心 | `dp[i]=max(dp[i-1], dp[i-2]+v)` | 拆两段线性 | 后序 + 双状态 |
| 空间 | O(1) 滚动 | O(1) 滚动 | O(h) 递归 |

---

## 四、综合拓展 DP（4 题）

---

### 221. 最大正方形 ⭐⭐⭐

[LeetCode 221](https://leetcode.cn/problems/maximal-square/)

- **核心**：`dp[i][j]` = 以 `(i,j)` 为右下角的最大正方形边长
- **转移**：`dp[i][j] = min(上, 左, 左上) + 1`（木桶效应）
- **易错**：返回 **边长²（面积）**；三数取 min 需嵌套；首行首列单独处理

```cpp
int maximalSquare(vector<vector<char>>& matrix) {
    int m = matrix.size(), n = matrix[0].size(), maxLen = 0;
    vector<vector<int>> dp(m, vector<int>(n, 0));
    for (int i = 0; i < m; i++)
        for (int j = 0; j < n; j++)
            if (matrix[i][j] == '1') {
                if (i == 0 || j == 0) dp[i][j] = 1;
                else dp[i][j] = min({dp[i-1][j], dp[i][j-1], dp[i-1][j-1]}) + 1;
                maxLen = max(maxLen, dp[i][j]);
            }
    return maxLen * maxLen;   // 🔑 返回面积不是边长
}
```

> 口诀：**正方形取三方最小加一，首行首列有 1 就是 1，最后边长平方返面积**

---

### 132. 分割回文串 II ⭐⭐

[LeetCode 132](https://leetcode.cn/problems/palindrome-partitioning-ii/)

- **核心**：组合 DP——先区间 DP 预处理回文表，再线性 DP 求最小分割
- **易错**：最坏 `dp[i] = i`（每个字符切一次）；`isPal[0][i]` 整段回文则 `dp[i]=0`

```cpp
int minCut(string s) {
    int n = s.size();
    // ① 区间 DP 预处理回文
    vector<vector<bool>> isPal(n, vector<bool>(n, false));
    for (int len = 1; len <= n; len++)
        for (int i = 0; i + len <= n; i++) {
            int j = i + len - 1;
            if (s[i] == s[j])
                isPal[i][j] = (len <= 2) ? true : isPal[i + 1][j - 1];
        }

    // ② 线性 DP 求最小分割
    vector<int> dp(n, n);
    for (int i = 0; i < n; i++) {
        if (isPal[0][i]) { dp[i] = 0; continue; }   // 整段回文不用切
        for (int j = 0; j < i; j++)
            if (isPal[j + 1][i])
                dp[i] = min(dp[i], dp[j] + 1);
    }
    return dp[n - 1];
}
```

> 口诀：**先预判回文，再算最少切；整段回文不用切，否则找前驱加一**

---

### 698. 划分为 k 个相等的子集 & 473. 火柴拼正方形 ⭐⭐

[LeetCode 698](https://leetcode.cn/problems/partition-to-k-equal-sum-subsets/) · [LeetCode 473](https://leetcode.cn/problems/matchsticks-to-square/)

- **核心**：状态压缩 DP——`mask` 每位标记元素是否已选，`dp[mask]` 记录当前子集的剩余和
- **适用**：`n ≤ 16`；473 是 698 的 `k=4` 特例

```
前置：sum % k != 0 → return false
target = sum / k
dp[mask] = 当前未装满子集的累计和
子集装满 target → dp 重置为 0，开新子集
```

```cpp
bool canPartitionKSubsets(vector<int>& nums, int k) {
    int sum = accumulate(nums.begin(), nums.end(), 0);
    if (sum % k != 0) return false;
    int target = sum / k, n = nums.size(), total = 1 << n;

    vector<int> dp(total, -1);
    dp[0] = 0;
    for (int mask = 0; mask < total; mask++) {
        if (dp[mask] == -1) continue;
        for (int i = 0; i < n; i++) {
            if (mask & (1 << i)) continue;
            if (dp[mask] + nums[i] > target) continue;
            int newMask = mask | (1 << i);
            dp[newMask] = (dp[mask] + nums[i] == target) ? 0 : dp[mask] + nums[i];
        }
    }
    return dp[total - 1] == 0;
}

// 473 火柴拼正方形：直接调用上面的 canPartitionKSubsets(matchsticks, 4)
```

> 口诀：**状压 DP 用掩码，n 小才适用；凑满 target 就重置，最后看全选是否归零**

---

## 五、完整题型 → DP 模型 映射

| 题型 | DP 模型 | 遍历要点 |
|------|---------|---------|
| 单串子序列 | 一维 DP | `i` 正序，连续只看 `i-1` |
| 双串匹配 | 二维 DP | `dp[i][j]`，下标错位一位 |
| 回文子序列 | 区间 DP | **先长度后起点** |
| 回文子串 | 中心扩展 | 奇数+偶数两种中心 |
| 线性选/不选 | 一维 DP | `dp[i] = max(dp[i-1], dp[i-2]+v)` |
| 环形选/不选 | 拆两段线性 | `[0,n-2]` vs `[1,n-1]` |
| 树形选/不选 | 树形 DP 后序 | 每节点返回双状态 |
| 矩阵最大正方形 | 二维 DP | `min(上,左,左上)+1` |
| 分割回文串 II | 区间+线性组合 | 先预判回文再 dp |
| k 等分子集 | 状压 DP | `mask` 枚举子集 |

---

## 六、机试频率排行

| 题号 | 题目 | 频率 | 难度 |
|------|------|------|------|
| 300 | 最长递增子序列 | ⭐⭐⭐⭐⭐ | 中等 |
| 198 | 打家劫舍 | ⭐⭐⭐⭐⭐ | 中等 |
| 1143 | 最长公共子序列 | ⭐⭐⭐⭐ | 中等 |
| 221 | 最大正方形 | ⭐⭐⭐⭐ | 中等 |
| 647 | 回文子串 | ⭐⭐⭐⭐ | 中等 |
| 213 | 打家劫舍 II | ⭐⭐⭐ | 中等 |
| 516 | 最长回文子序列 | ⭐⭐⭐ | 中等 |
| 337 | 打家劫舍 III | ⭐⭐⭐ | 中等 |
| 718 | 最长重复子数组 | ⭐⭐⭐ | 中等 |
| 674 | 最长连续递增 | ⭐⭐ | 简单 |
| 132 | 分割回文串 II | ⭐⭐ | 困难 |
| 698 | 划分为 k 子集 | ⭐⭐ | 中等 |
| 473 | 火柴拼正方形 | ⭐⭐ | 中等 |

---

## 七、考前一分钟速记

```
连续只看前一格，非连续看前面所有
双串匹配错一位，相等左上不等取左右
连续不等就清零，非连续取 max
回文区间先长后始，回文子串中心扩展
打家劫舍选不选，环形拆两段，树形后序双状态
最大正方形三方取最小，再加一
分割回文先预判，再 dp
状压 DP 看 n，≤16 才上掩码
```

---

> **状态**：DP 全部板块 ✅ 锁死 | 与 [07-动态规划基础与背包](07-动态规划基础与背包.md) 合璧，DP 全覆盖
