# 二叉树进阶 · 机试核心速记手册

> 今日覆盖：12 题 | 路径回溯 / LCA / 构造 / BST 性质 | 机试拉分区

---

## 一、本板块三大核心考点

| 考点 | 核心思想 | 代表题 | 频率 |
|------|----------|--------|------|
| **路径总和系列** | 前序 + 回溯 | 112 / 113 / 437 | ⭐⭐⭐⭐ |
| **LCA 与构造** | 后序回溯 / 区间递归 | 236 / 105 / 106 / 654 | ⭐⭐⭐⭐⭐ |
| **BST 性质** | 中序有序 + 左小右大 | 98 / 235 / 530 / 501 / 700 | ⭐⭐⭐⭐⭐ |

---

## 二、三大万能模板

### 模板 1：前序 + 回溯（路径问题）

```cpp
void backtrack(TreeNode* cur, vector<int>& path, vector<vector<int>>& res) {
    if (!cur) return;
    path.push_back(cur->val);                          // ① 加入

    if (!cur->left && !cur->right && 满足条件)          // ② 叶子判断
        res.push_back(path);

    if (cur->left)  { backtrack(cur->left,  res); path.pop_back(); }   // ③ 回溯
    if (cur->right) { backtrack(cur->right, res); path.pop_back(); }
}
```

### 模板 2：后序 + 自底向上（LCA / 高度 / 平衡）

```cpp
ResType dfs(TreeNode* root) {
    if (!root) return 基准值;
    ResType L = dfs(root->left);
    ResType R = dfs(root->right);
    // 后序位置：合并左右结果
    return 根据 L 和 R 决策;
}
```

### 模板 3：区间构造（105 / 106 / 654）

```cpp
TreeNode* build(数组, int l, int r) {
    if (l > r) return nullptr;
    int rootIdx = 找根位置(l, r);                     // 前序首 / 后序尾 / 最大值
    TreeNode* root = new TreeNode(值);
    root->left  = build(数组, l, rootIdx - 1);
    root->right = build(数组, rootIdx + 1, r);
    return root;
}
```

---

## 三、12 题逐题复盘

### 第一阶段：路径总和系列（3 题）

---

#### 112. 路径总和 I ⭐⭐

[LeetCode 112](https://leetcode.cn/problems/path-sum/)

- **核心**：前序递归，遇到叶子判断 `root->val == targetSum`
- **易错**：必须是叶子节点；节点值可为负，不能提前剪枝

```cpp
bool hasPathSum(TreeNode* root, int targetSum) {
    if (!root) return false;
    if (!root->left && !root->right)          // 必须是叶子
        return root->val == targetSum;
    return hasPathSum(root->left,  targetSum - root->val)
        || hasPathSum(root->right, targetSum - root->val);
}
```

> 口诀：**路径总和 I，叶子来判断；和等就返回，不等继续转**

---

#### 113. 路径总和 II ⭐⭐

[LeetCode 113](https://leetcode.cn/problems/path-sum-ii/)

- **核心**：前序 + 显式回溯，用 `vector<int>` 记录当前路径
- **易错**：回溯位置在递归返回后，不是叶子 return 之前；全局变量需先 clear

```cpp
vector<vector<int>> res; vector<int> path;

void dfs(TreeNode* cur, int target) {
    if (!cur) return;
    path.push_back(cur->val);

    if (!cur->left && !cur->right && cur->val == target)
        res.push_back(path);                     // 叶子匹配 → 存

    if (cur->left)  { dfs(cur->left,  target - cur->val); path.pop_back(); }
    if (cur->right) { dfs(cur->right, target - cur->val); path.pop_back(); }
}
```

> 口诀：**路径总和 II，前序加路径；叶子判和等，递归完要回溯**

---

#### 437. 路径总和 III ⭐⭐

[LeetCode 437](https://leetcode.cn/problems/path-sum-iii/)

- **核心**：
  - **暴力法**（机试首选）：双重递归——外层遍历起点，内层以该点向下计数
  - **最优法**（面试加分）：前缀和 + 哈希表，`prefixSumCount[0] = 1`
- **易错**：必须用 `long long` 防溢出；路径只能向下

```cpp
// 暴力法：双重递归
int pathSumFrom(TreeNode* root, long long target) {
    if (!root) return 0;
    int cnt = (root->val == target);
    cnt += pathSumFrom(root->left,  target - root->val);
    cnt += pathSumFrom(root->right, target - root->val);
    return cnt;
}
int pathSum(TreeNode* root, int targetSum) {
    if (!root) return 0;
    return pathSumFrom(root, targetSum)
         + pathSum(root->left, targetSum)
         + pathSum(root->right, targetSum);
}
```

```cpp
// 前缀和法（O(n)）
unordered_map<long long, int> pre;
int dfs(TreeNode* cur, long long sum, int target) {
    if (!cur) return 0;
    sum += cur->val;
    int cnt = pre[sum - target];
    pre[sum]++;
    cnt += dfs(cur->left, sum, target) + dfs(cur->right, sum, target);
    pre[sum]--;                              // 回溯
    return cnt;
}
// 调用前：pre[0] = 1;
```

> 口诀：**路径总和 III，双重递归最简单；前缀和更优，pre[0]=1 别忘**

---

### 第二阶段：LCA 与构造（4 题）

---

#### 236. 二叉树的最近公共祖先 ⭐⭐⭐

[LeetCode 236](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-tree/)

- **核心**：后序遍历自底向上，左右都找到 → 当前即 LCA
- **易错**：必须后序；`root == p || root == q` 直接返回

```cpp
TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
    if (!root || root == p || root == q) return root;   // 终止

    TreeNode* L = lowestCommonAncestor(root->left,  p, q);
    TreeNode* R = lowestCommonAncestor(root->right, p, q);

    if (L && R) return root;   // 左右都有 → 当前是 LCA
    return L ? L : R;           // 哪边有返回哪边
}
```

> 口诀：**LCA 后序看，左右都有返当前；哪边有往哪边传**

---

#### 105. 从前序与中序遍历序列构造二叉树 ⭐⭐⭐

[LeetCode 105](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/)

- **核心**：前序首元素 = 根；中序根位置分左右；哈希表 O(1) 定位
- **易错**：区间边界算错是最大失分点；必须用哈希表加速

```cpp
unordered_map<int, int> inMap;
TreeNode* build(vector<int>& pre, int pL, int pR,
                vector<int>& in,  int iL, int iR) {
    if (pL > pR) return nullptr;

    int rootVal = pre[pL];                       // 前序第一个 = 根
    TreeNode* root = new TreeNode(rootVal);
    int idx = inMap[rootVal];                    // 中序中定位
    int leftSize = idx - iL;

    root->left  = build(pre, pL + 1,            pL + leftSize,    in, iL, idx - 1);
    root->right = build(pre, pL + leftSize + 1, pR,               in, idx + 1, iR);
    return root;
}
```

> 口诀：**前序找根，中序分左右；哈希快定位，递归造整树**

---

#### 106. 从中序与后序遍历序列构造二叉树 ⭐⭐

[LeetCode 106](https://leetcode.cn/problems/construct-binary-tree-from-inorder-and-postorder-traversal/)

- **核心**：与 105 几乎相同，区别仅在后序最后一个元素是根
- **易错**：区间划分与 105 不同

**105 vs 106 对比速查：**

| | 根位置 | 左子树区间 | 右子树区间 |
|--|--------|-----------|-----------|
| 105 前序+中序 | 前序首 `pre[pL]` | 前序 `[pL+1, pL+leftSize]` | 前序 `[pL+leftSize+1, pR]` |
| 106 中序+后序 | 后序尾 `post[pR]` | 后序 `[pL, pL+leftSize-1]` | 后序 `[pL+leftSize, pR-1]` |

```cpp
int rootVal = postorder[pR];                    // 后序最后 = 根
root->left  = build(in, iL, idx-1,  post, pL,                pL+leftSize-1);
root->right = build(in, idx+1, iR, post, pL+leftSize,        pR-1);
```

> 口诀：**后序找根，中序分左右；区间要算对，递归往下造**

---

#### 654. 最大二叉树 ⭐

[LeetCode 654](https://leetcode.cn/problems/maximum-binary-tree/)

- **核心**：区间内找最大值作为根，左右递归
- **易错**：终止条件 `l > r`；最大值查找区间是 `[l, r]`

```cpp
TreeNode* build(vector<int>& nums, int l, int r) {
    if (l > r) return nullptr;
    int maxIdx = l;
    for (int i = l; i <= r; i++)
        if (nums[i] > nums[maxIdx]) maxIdx = i;
    TreeNode* root = new TreeNode(nums[maxIdx]);
    root->left  = build(nums, l, maxIdx - 1);
    root->right = build(nums, maxIdx + 1, r);
    return root;
}
```

> 口诀：**最大二叉树，区间找最大；左边构左树，右边构右树**

---

### 第三阶段：二叉搜索树（5 题）

---

#### 700. 二叉搜索树中的搜索 ⭐

[LeetCode 700](https://leetcode.cn/problems/search-in-a-binary-search-tree/)

- **核心**：利用 BST 左小右大定向搜索
- **易错**：不要当普通二叉树遍历；迭代法 O(1) 空间

```cpp
// 迭代（O(1) 空间）
TreeNode* searchBST(TreeNode* root, int val) {
    while (root) {
        if (root->val == val) return root;
        root = root->val > val ? root->left : root->right;
    }
    return nullptr;
}
```

> 口诀：**BST 搜索很简单，左小右大记心间；大了往左小往右**

---

#### 98. 验证二叉搜索树 ⭐⭐⭐

[LeetCode 98](https://leetcode.cn/problems/validate-binary-search-tree/)

- **核心**：
  - **中序法**（机试首选）：中序遍历结果严格递增
  - **范围法**（面试加分）：给每个节点合法范围 `(min, max)`
- **易错**：不能只比左右孩子，要保证整棵子树都合法；用 `long long` 防极值

```cpp
// 中序法
long long pre = LONG_MIN;
bool isValidBST(TreeNode* root) {
    if (!root) return true;
    if (!isValidBST(root->left)) return false;      // 左
    if (root->val <= pre) return false;              // 根（必须严格大于）
    pre = root->val;
    return isValidBST(root->right);                  // 右
}

// 范围法
bool helper(TreeNode* root, long long min, long long max) {
    if (!root) return true;
    if (root->val <= min || root->val >= max) return false;
    return helper(root->left, min, root->val)
        && helper(root->right, root->val, max);
}
```

> 口诀：**验证 BST，中序递增是真理；或者给个范围框，左右子树都要扛**

---

#### 530. 二叉搜索树的最小绝对差 ⭐

[LeetCode 530](https://leetcode.cn/problems/minimum-absolute-difference-in-bst/)

- **核心**：BST 中序有序 → 最小差必在相邻元素
- **易错**：不要遍历所有节点对（O(n²)）；中序递增，差为正不用取绝对值

```cpp
int minDiff = INT_MAX;
TreeNode* pre = nullptr;
int getMinimumDifference(TreeNode* root) {
    if (!root) return minDiff;
    getMinimumDifference(root->left);
    if (pre) minDiff = min(minDiff, root->val - pre->val);
    pre = root;
    getMinimumDifference(root->right);
    return minDiff;
}
```

> 口诀：**BST 最小差，中序相邻比一比**

---

#### 501. 二叉搜索树中的众数 ⭐⭐

[LeetCode 501](https://leetcode.cn/problems/find-mode-in-binary-search-tree/)

- **核心**：中序有序 → 相同值连续出现 → O(1) 空间边遍历边统计
- **易错**：众数可能有多个，出现更高频率时清空结果再添加

```cpp
vector<int> res;
int maxCnt = 0, curCnt = 0;
TreeNode* pre = nullptr;

vector<int> findMode(TreeNode* root) {
    dfs(root);
    return res;
}

void dfs(TreeNode* cur) {
    if (!cur) return;
    dfs(cur->left);
    curCnt = (pre && pre->val == cur->val) ? curCnt + 1 : 1;  // 统计连续
    if (curCnt > maxCnt) { maxCnt = curCnt; res = {cur->val}; }
    else if (curCnt == maxCnt) res.push_back(cur->val);
    pre = cur;
    dfs(cur->right);
}
```

> 口诀：**BST 找众数，中序连续数频率；超过清空重记录，相等追加别遗漏**

---

#### 235. 二叉搜索树的最近公共祖先 ⭐⭐

[LeetCode 235](https://leetcode.cn/problems/lowest-common-ancestor-of-a-binary-search-tree/)

- **核心**：利用 BST 性质——p/q 同在左/右侧则下探，在两侧则当前即 LCA
- **与 236 对比**：BST 版 O(h)，不需要后序遍历

| | 236 普通二叉树 | 235 BST |
|--|--------------|---------|
| 核心 | 后序回溯 | BST 性质定向 |
| 时间 | O(n) | O(h) |
| 空间 | O(h) | O(h) |

```cpp
TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
    if (!root) return nullptr;
    if (root->val > p->val && root->val > q->val)       // 都在左
        return lowestCommonAncestor(root->left, p, q);
    if (root->val < p->val && root->val < q->val)       // 都在右
        return lowestCommonAncestor(root->right, p, q);
    return root;    // 在两侧，或其中一个是当前 → 就是 LCA
}
```

> 口诀：**BST 公共祖先，左小右大来判断；都在左边往左走，都在右边往右转，两边都有返回根**

---

## 四、题目类型 → 遍历方式 速查

| 题目 | 遍历 | 理由 |
|------|------|------|
| 路径总和 I/II/III | 前序 | 路径从根开始 |
| LCA（236） | 后序 | 自底向上合并 |
| BST LCA（235） | 前序（定向） | 利用 BST 性质 |
| 前序+中序构造 | 前序 | 先确定根 |
| 中序+后序构造 | 后序 | 从后向前 |
| 验证 BST | 中序 | 利用有序性 |
| BST 最小差 | 中序 | 相邻元素才可能是最小差 |
| BST 众数 | 中序 | 相同值连续 |

---

## 五、考前避坑清单

| # | 坑 | 说明 |
|---|-----|------|
| 1 | **整数溢出** | 路径和 / 范围边界 一律用 `long long` |
| 2 | **回溯位置** | pop 在递归返回后，不是叶子 return 前 |
| 3 | **区间构造边界** | 前序/后序的左右子树区间起止不同 |
| 4 | **BST 验证** | 不能只比左右孩子，要用中序严格递增或范围法 |
| 5 | **叶子判断** | 路径题必须到叶子，忘了直接判错 |
| 6 | **中序 pre 更新** | 中序处理完当前节点必须更新 pre |

---

## 六、机试频率排行

| 题号 | 题目 | 频率 | 难度 |
|------|------|------|------|
| 236 | 二叉树 LCA | ⭐⭐⭐⭐⭐ | 中等 |
| 105 | 前序+中序构造 | ⭐⭐⭐⭐ | 中等 |
| 98 | 验证 BST | ⭐⭐⭐⭐ | 中等 |
| 235 | BST LCA | ⭐⭐⭐⭐ | 中等 |
| 113 | 路径总和 II | ⭐⭐⭐ | 中等 |
| 106 | 中序+后序构造 | ⭐⭐⭐ | 中等 |
| 112 | 路径总和 I | ⭐⭐⭐ | 简单 |
| 530 | BST 最小差 | ⭐⭐⭐ | 简单 |
| 437 | 路径总和 III | ⭐⭐⭐ | 中等 |
| 501 | BST 众数 | ⭐⭐ | 简单 |
| 654 | 最大二叉树 | ⭐⭐ | 中等 |
| 700 | BST 搜索 | ⭐ | 简单 |

---

## 七、考前一分钟速记

```
路径问题   →  前序+回溯，叶子才判和
LCA       →  后序自底向上，左右都有返当前
构造题     →  找根+分区间，边界是命门
BST 万能   →  中序有序，左小右大
整数累加   →  long long，别溢出
```

---

> **状态**：二叉树进阶板块 ✅ 锁死 | **下一板块**：回溯算法 / 贪心
